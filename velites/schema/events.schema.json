{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Event",
  "description": "The velites/json1 event stream: exactly these eleven event types, NDJSON\non stdout, one JSON object per line.",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "session"
        }
      },
      "$ref": "#/$defs/SessionEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "agent_start"
        }
      },
      "$ref": "#/$defs/AgentStartEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "agent_end"
        }
      },
      "$ref": "#/$defs/AgentEndEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "turn_start"
        }
      },
      "$ref": "#/$defs/TurnStartEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "turn_end"
        }
      },
      "$ref": "#/$defs/TurnEndEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "message_start"
        }
      },
      "$ref": "#/$defs/MessageStartEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "message_end"
        }
      },
      "$ref": "#/$defs/MessageEndEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "auto_retry_start"
        }
      },
      "$ref": "#/$defs/AutoRetryStartEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "tool_execution_start"
        }
      },
      "$ref": "#/$defs/ToolExecutionStartEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "tool_execution_end"
        }
      },
      "$ref": "#/$defs/ToolExecutionEndEvent",
      "required": [
        "type"
      ]
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "outputs_validation"
        }
      },
      "$ref": "#/$defs/OutputsValidationEvent",
      "required": [
        "type"
      ]
    }
  ],
  "$defs": {
    "AgentEndEvent": {
      "description": "`agent_end` event: final line of the stream. `error` is present only when\nthe run ended with an unrecovered model error (exit code stays 0, matching\nPi; the Host judges failure from the event stream). `reason` is present\nonly on budget exhaustion or cancellation (exit code also stays 0 — see\n`cancel.rs`).\n\nSchema v2: unlike Pi, this event does NOT carry the full `messages`\nhistory — no Host consumer reads it, and every message already appeared\nin its own `message_end` / `tool_execution_end` event.",
      "type": "object",
      "properties": {
        "error": {
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "anyOf": [
            {
              "$ref": "#/$defs/EndReason"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "AgentStartEvent": {
      "description": "`agent_start` event: emitted once before the first turn.",
      "type": "object"
    },
    "AutoRetryStartEvent": {
      "description": "`auto_retry_start` event (pi-compatible): emitted right after the error\n`message_end` of a failed transient attempt, before the backoff sleep.\nThere is intentionally no `auto_retry_end` — Pi doesn't have one either;\nthe Host's `fold_model_error` treats the next successful assistant\n`message_end` (`stopReason=stop|toolUse`) as \"recovered\".",
      "type": "object",
      "properties": {
        "attempt": {
          "description": "1-based retry counter (pi wire field).",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "delayMs": {
          "description": "Backoff delay before the retry fires, in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "error": {
          "description": "Short description of the transient error that triggered the retry.",
          "type": "string"
        },
        "maxAttempts": {
          "description": "Total attempts allowed, initial call included.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "attempt",
        "maxAttempts",
        "delayMs",
        "error"
      ]
    },
    "ContentBlock": {
      "description": "Content block of a message (pi-compatible shapes).",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "text": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "text"
            }
          },
          "required": [
            "type",
            "text"
          ]
        },
        {
          "type": "object",
          "properties": {
            "thinking": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "thinking"
            }
          },
          "required": [
            "type",
            "thinking"
          ]
        },
        {
          "type": "object",
          "properties": {
            "arguments": true,
            "id": {
              "description": "Filled by the provider (stub auto-generates `call-<n>-<i>`).",
              "type": "string",
              "default": ""
            },
            "name": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "toolCall"
            }
          },
          "required": [
            "type",
            "name",
            "arguments"
          ]
        }
      ]
    },
    "EndReason": {
      "description": "Why the run ended early. Absent on a normal completion or an unrecovered\nmodel error (the `error` field covers that path); present only when the\nharness itself cut the run short (design §5 controllability).",
      "oneOf": [
        {
          "description": "A run budget (turns / tokens / wall-clock deadline) was exhausted;\nthe model got one wrap-up turn before the loop ended.",
          "type": "string",
          "const": "budget_exceeded"
        },
        {
          "description": "SIGTERM requested cancellation; the loop stopped at the next\ncheckpoint (turn boundary or after the current tool execution).",
          "type": "string",
          "const": "cancelled"
        }
      ]
    },
    "Message": {
      "description": "One conversation message. Assistant metadata (`usage`, `provider`, `model`,\n`stopReason`, `errorMessage`, `timing`) and tool-result metadata (`toolCallId`,\n`toolName`, `isError`) are only present on the roles they apply to; `None`\nfields are skipped on the wire.",
      "type": "object",
      "properties": {
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ContentBlock"
          }
        },
        "errorMessage": {
          "type": [
            "string",
            "null"
          ]
        },
        "isError": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "$ref": "#/$defs/Role"
        },
        "stopReason": {
          "anyOf": [
            {
              "$ref": "#/$defs/StopReason"
            },
            {
              "type": "null"
            }
          ]
        },
        "timing": {
          "description": "Request-level timing (velites extension); only set by real providers\non successful completions — the stub provider and error events omit it.",
          "anyOf": [
            {
              "$ref": "#/$defs/RequestTiming"
            },
            {
              "type": "null"
            }
          ]
        },
        "toolCallId": {
          "type": [
            "string",
            "null"
          ]
        },
        "toolName": {
          "type": [
            "string",
            "null"
          ]
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/$defs/Usage"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "role",
        "content"
      ]
    },
    "MessageEndEvent": {
      "description": "`message_end` event: the final assistant message. This is the token-metering\nand failure-detection record — `usage`, `provider`, `model`, `stopReason`,\n`content`, `errorMessage` all live here.",
      "type": "object",
      "properties": {
        "message": {
          "$ref": "#/$defs/Message"
        }
      },
      "required": [
        "message"
      ]
    },
    "MessageStartEvent": {
      "description": "`message_start` event: assistant message skeleton (role/provider/model\nknown, content empty).",
      "type": "object",
      "properties": {
        "message": {
          "$ref": "#/$defs/Message"
        }
      },
      "required": [
        "message"
      ]
    },
    "OutputsValidationEvent": {
      "description": "`outputs_validation` event (velites extension, design §5 输出自检):\nemitted right before `agent_end` whenever `--require-output` was given and\nthe run ended normally or by budget exhaustion — always emitted in that\ncase so the Host can decide explicitly, with `missing` listing the\ndeclared artifacts (relative paths as passed on the CLI) that still do not\nexist after the single remediation turn. Not emitted on cancellation or\nunrecovered model error.\n\n#443: `mode` distinguishes the check that ran — `contract` when a skill's\n`references/output-contract.md` contract block drove content validation,\n`existence` when none was parseable (legacy semantics). `violations`\ncarries the contract violations (empty in existence mode or when all\nrules hold); both fields are always present to keep the wire shape\nstable.",
      "type": "object",
      "properties": {
        "missing": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "mode": {
          "type": "string"
        },
        "violations": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "missing",
        "mode",
        "violations"
      ]
    },
    "RequestTiming": {
      "description": "Request-level timing of one LLM completion (velites extension; Pi has no\nequivalent). Attached to the assistant message of the SUCCESSFUL attempt —\nfailed transient attempts surface as pi-compatible error `message_end`\nevents without timing. All durations are wall-clock milliseconds measured\nby the provider:\n\n- `ttfbMs`: POST sent → first SSE `data:` chunk (for the non-streaming\n  JSON fallback dialect: → response headers);\n- `streamMs`: first → last SSE `data:` chunk (`[DONE]` or connection end);\n- `totalMs`: POST sent → stream end.\n\nTokens-per-second is NOT stored: consumers derive it as\n`usage.output / (streamMs / 1000)`.",
      "type": "object",
      "properties": {
        "streamMs": {
          "description": "First → last stream chunk, in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "totalMs": {
          "description": "POST sent → stream end, in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "ttfbMs": {
          "description": "POST sent → first response chunk, in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "ttfbMs",
        "streamMs",
        "totalMs"
      ]
    },
    "Role": {
      "description": "Message role. `ToolResult` serializes as `toolResult` (pi-compatible).",
      "type": "string",
      "enum": [
        "user",
        "assistant",
        "toolResult"
      ]
    },
    "SessionEvent": {
      "description": "`session` event: first line of the stream.",
      "type": "object",
      "properties": {
        "sessionId": {
          "description": "Session identifier (`--name`, or a generated fallback).",
          "type": "string"
        },
        "timestamp": {
          "description": "Unix seconds at session start.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "sessionId",
        "timestamp"
      ]
    },
    "StopReason": {
      "description": "Stop reason of an assistant message. Wire names are camelCase to match Pi\n(`stop` / `length` / `toolUse` / `error` / `aborted`).",
      "type": "string",
      "enum": [
        "stop",
        "length",
        "toolUse",
        "error",
        "aborted"
      ]
    },
    "ToolExecutionEndEvent": {
      "description": "`tool_execution_end` event. `output_bytes` measures the tool's output\nvolume (stdout+stderr for bash, written/returned bytes for write/read);\nit is a measurement field only — no truncation happens in M1. `timing`\ncarries the #469 phase decomposition (`spawnMs` / `firstByteMs` /\n`restMs` / `reapMs`); it is skipped on the wire for tool errors raised\nbefore any measurement (argument validation, guard rejection) — exactly\nthe `RequestTiming` convention on `message_end`.",
      "type": "object",
      "properties": {
        "isError": {
          "type": "boolean"
        },
        "output_bytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "result": {
          "$ref": "#/$defs/ToolResultData"
        },
        "timing": {
          "anyOf": [
            {
              "$ref": "#/$defs/ToolTiming"
            },
            {
              "type": "null"
            }
          ]
        },
        "toolCallId": {
          "type": "string"
        },
        "toolName": {
          "type": "string"
        }
      },
      "required": [
        "toolCallId",
        "toolName",
        "result",
        "isError",
        "output_bytes"
      ]
    },
    "ToolExecutionStartEvent": {
      "description": "`tool_execution_start` event.",
      "type": "object",
      "properties": {
        "args": true,
        "toolCallId": {
          "type": "string"
        },
        "toolName": {
          "type": "string"
        }
      },
      "required": [
        "toolCallId",
        "toolName",
        "args"
      ]
    },
    "ToolResultData": {
      "description": "Result payload shared by `tool_execution_end` (`result.content`).",
      "type": "object",
      "properties": {
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ContentBlock"
          }
        }
      },
      "required": [
        "content"
      ]
    },
    "ToolTiming": {
      "description": "Phase timing of one tool execution (velites extension, #469). The whole\nfield is optional per tool and every phase inside it is optional:\nabsent phases are skipped on the wire, never null. Durations are\nwall-clock milliseconds. `totalMs` is measured once at the\n`ToolKind::execute` dispatch boundary — every tool kind gets it, and\nin-process tools (`read` / `write` / `uuid` / `validate`) carry\nnothing else.\n\nPhase boundaries for `bash` (the subprocess phases bash fills itself):\n\n- `totalMs`: dispatch start → result ready (the decomposition base:\n  `total = spawnMs + firstByteMs + restMs + reapMs` on the happy path);\n- `spawnMs`: `Command::spawn` start → child pid returned (process\n  creation, sandbox wrapper exec included);\n- `firstByteMs`: spawn returned → first byte read from the child's\n  stdout/stderr pipes — whichever stream produced a byte first. This\n  is when the HARNESS observed the first byte, not when the child\n  wrote it (safe today because the agent loop awaits tools\n  sequentially; see bash.rs's observation caveat). A scheduler-level\n  gap between the child writing and the harness reading — exactly\n  what a kernel pipe-path sleep (#469 spindump: `lck_mtx_sleep`)\n  produces — lands in this phase, so the read-side semantics are the\n  metric, not a distortion of it. The phase covers the child's entire\n  prelude — bash parsing, an internal `<<EOF` heredoc write,\n  interpreter startup, the script's first side effect — so a stall\n  inside the child (the #469 blocking candidate: bash `heredoc_write`\n  → `write()`) surfaces here exactly. Absent when the child produced\n  no output at all;\n- `restMs`: first byte → child exit observed (the steady run: output\n  streaming, waiting for completion). On a no-output child this covers\n  the whole output window instead (there is no first byte to start\n  from). On the timeout/cancel path it ends where the timeout fired —\n  killing and reaping the group is `reapMs`, not part of the run;\n- `reapMs`: exit/timeout observed → process group reaped (present on\n  the timeout/cancel kill path);\n- `requestedTimeoutMs`: the bash `timeout` argument AFTER clamping into\n  [1s, 1h] (default 120s), in milliseconds — the effective ceiling the\n  harness enforced on this call. Absent for tools without a timeout\n  parameter. Analysis joins it against `totalMs` / `reapMs` to separate\n  \"the model asked for a long ceiling\" from \"the call hung within a\n  normal one\" (#469: models raise `timeout` after consecutive\n  failures, so the requested value is a leading explanation variable\n  for long-tail variance).\n\nPermanent harness capability (like `RequestTiming`): no toggles, no\naggregation — phase analytics stay on the Host side.",
      "type": "object",
      "properties": {
        "firstByteMs": {
          "description": "Spawn returned → first output byte observed (earliest of the stdout\nand stderr readers), in milliseconds; absent when the child produced\nno output.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "reapMs": {
          "description": "Group termination + reap phase (kill/TERM → KILL grace → reaped), in\nmilliseconds; absent on a natural exit.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "requestedTimeoutMs": {
          "description": "The enforced `timeout` ceiling (bash: the clamped model-supplied\nargument), in milliseconds; absent for tools without a timeout.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "restMs": {
          "description": "First output byte → child exit observed, in milliseconds; on a\nno-output child this covers the whole output window instead. On the\ntimeout/cancel path it ends at the timeout; group termination is\naccounted as `reapMs`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "spawnMs": {
          "description": "Process creation phase (`Command::spawn`) duration, in milliseconds.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "totalMs": {
          "description": "Tool dispatch start → result ready, in milliseconds.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      }
    },
    "TurnEndEvent": {
      "description": "`turn_end` event: turn boundary marker only (schema v2). Pi re-serializes\nthe assistant `message` and its `toolResults` here; velites does not — the\nassistant message is already carried by this turn's `message_end`, and the\ntool results by the `tool_execution_end.result.content` events.",
      "type": "object",
      "properties": {
        "turnIndex": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "turnIndex"
      ]
    },
    "TurnStartEvent": {
      "description": "`turn_start` event.",
      "type": "object",
      "properties": {
        "turnIndex": {
          "description": "1-based turn counter.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "turnIndex"
      ]
    },
    "Usage": {
      "description": "Token usage of one assistant message. `cacheRead` is camelCase on the wire.\n\nPi-aligned semantics: `input` EXCLUDES cache-read tokens (the provider's\n`prompt_tokens` includes them; subtracting avoids double-billing the\ncached part), so `input + cacheRead` reconstructs `prompt_tokens`.",
      "type": "object",
      "properties": {
        "cacheRead": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "input": {
          "description": "Non-cached input tokens (pi-aligned; excludes `cacheRead`).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "output": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "input",
        "output",
        "cacheRead"
      ]
    }
  }
}
