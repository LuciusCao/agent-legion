# deploy/ 的 compose 变量替换样例：复制为 deploy/.env 并填值（.env 已被
# git/docker 忽略）。compose.host.yaml 的 rustfs 服务与 host 的材料存储
# 共用同一组凭据（materials-and-runs 设计 §6.1，infra env-only）。

# 材料对象存储凭据（rustfs 服务的 RUSTFS_ACCESS_KEY/SECRET_KEY 与 host 的
# AGENT_LEGION_S3_ACCESS_KEY/SECRET_KEY 共用；必填，无默认值）。
# 生成强随机值：openssl rand -hex 16 / openssl rand -base64 24
AGENT_LEGION_S3_ACCESS_KEY=
AGENT_LEGION_S3_SECRET_KEY=

# host 的材料 bucket（默认 agent-legion）；rustfs 首次启动后需建 bucket
# （init-worktree.sh 的建 bucket 逻辑或 aws s3 mb）。
# AGENT_LEGION_S3_BUCKET=agent-legion
