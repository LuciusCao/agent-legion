# deploy/ 的 compose 变量替换样例：复制为 deploy/.env 并填值（.env 已被
# git/docker 忽略）。compose.host.yaml 的本地对象存储服务（默认 seaweedfs，
# 逃生舱 rustfs）与 host 的材料存储共用同一组凭据（materials-and-runs 设计
# §6.1，infra env-only）。

# 本地后端选择：seaweedfs（默认）/ rustfs（#340 调研后默认切换；rustfs 为
# 存量卷迁移期的逃生舱）。切 rustfs 时需同步覆盖下方 ENDPOINT/
# PUBLIC_ENDPOINT 为 :9000 形态。
# AGENT_LEGION_LOCAL_S3_BACKEND=seaweedfs

# 本地对象存储三态开关：auto（默认）/ always / never。
#   always  无条件启动本地对象存储服务（旧版行为）
#   never   不启动（使用外部对象存储）
#   auto    按下方的 S3 配置自动判断：endpoint 指向本机（127.0.0.1 /
#           localhost / ::1 / compose 内部名 seaweedfs / rustfs）或未配置
#           任何 S3 → 启动；endpoint 指向外部地址，或只配 bucket/凭据而不配
#           endpoint（AWS S3 默认端点写法）→ 跳过并打一行原因日志。
# 判断逻辑见 scripts/local-s3-decide.sh，所有 prod-up / stack 入口共用；
# docker stack 形态的默认 endpoint 是 compose 注入的 http://seaweedfs:8333。
# auto 误判不会静默失败：后端启动自检（probe 的 DEGRADED 日志）与
# /api/health 的 storage.reachable 会暴露。
# AGENT_LEGION_LOCAL_S3=auto

# 材料对象存储凭据（本地后端容器与 host 的 AGENT_LEGION_S3_* 共用）。
# 本地后端形态必填（缺了时 prod-up 入口 fail-fast）；外部 S3 走默认凭据链
# （IAM role 等）时留空。生成强随机值：openssl rand -hex 16 / openssl rand -base64 24
AGENT_LEGION_S3_ACCESS_KEY=
AGENT_LEGION_S3_SECRET_KEY=

# host 的材料 bucket（默认 agent-legion）；本地后端首次启动后需建 bucket
# （init-worktree.sh 的建 bucket 逻辑或 aws s3 mb）。
# AGENT_LEGION_S3_BUCKET=agent-legion

# 后端直连的 S3 endpoint。不配时 docker stack 形态默认 http://seaweedfs:8333
# （compose 注入，指向本地 seaweedfs 容器；切 rustfs 后端时配
# http://rustfs:9000）。使用外部对象存储时设为远程地址（minio.example.com
# 等），local-s3-decide.sh 会同步跳过本地后端；用 AWS S3 默认端点时显式
# 留空（AGENT_LEGION_S3_ENDPOINT=，空值生效，不会被默认值盖掉）。
# AGENT_LEGION_S3_ENDPOINT=http://seaweedfs:8333

# presigned URL 的签发地址（浏览器直传 / remote worker 下载可达）；
# 默认 http://127.0.0.1:8333 匹配 seaweedfs 端口映射（SigV4 把 Host 签进
# 签名，签后不可改写），非本机访问或切 rustfs（:9000）时覆盖。
# AGENT_LEGION_S3_PUBLIC_ENDPOINT=http://127.0.0.1:8333
