# Copy to deploy/velites-provider.env (or point VELITES_PROVIDER_ENV_FILE at
# another 0600 file), then uncomment only the variables referenced by the
# mounted ~/.velites/models.json. Never commit the populated file.
#
# ANTHROPIC_API_KEY=<provider-key>
# SQAI_API_KEY=<provider-key>
