"""Legacy bootstrap tests have been replaced by test_executor_legacy_finalization.py.

The recurring `bootstrap_workspace_executor_defaults` finalizer was removed in
Phase 5; Workspace Agent/Pipeline configuration is now migrated once by
`finalize_legacy_executor_schema`.
"""
