# Phase 1-5 Workspace Executor Evidence Matrix

This matrix records the reverse audit of Phase 1-5 Workspace Executor architecture promises.

- `Verified` — the promise already meets the evidence rule and is registered as an invariant.
- `Gap` — the promise is approved but evidence is missing; linked to the task that will close it.
- `Deferred` — the promise is out of scope for Phase 1-5 migration evaluation.
- `Behavior-only` — the promise is tested but not elevated to a durable invariant.

## Matrix

| Promise | Boundary | Invariant ID | Quick evidence | Full evidence | Result | Follow-up task |
| --- | --- | --- | --- | --- | --- | --- |
| Generated frontend API types remain synchronized with the backend OpenAPI schema | contract | API-CONTRACT-001 | `scripts/generate-api-types.sh` | N/A | Verified | N/A |
| Routes, services, repositories, and executor adapters preserve dependency direction | layer | BOUNDARY-LAYER-001 | `scripts/check_architecture.py` | N/A | Verified | N/A |
| New services reach the database through the JobQueries facade; raw SQL, DB-primitive imports, and DSN path accesses under services are ratcheted down from the frozen baseline | data | BOUNDARY-DATA-001 | `scripts/architecture/service_data_boundary.py` | `tests/scripts/test_architecture_service_data_boundary.py` | Verified | N/A |
| schema_migrations records one row per version; upgrades run only the increment above max(applied) | db | DB-SCHEMA-001 | `tests/db/test_schema_version_registry.py` | `tests/db/test_db.py::test_database_initialization_records_postgres_schema` | Verified | N/A |
| Video and job filesystem operations stay inside managed roots and reject symlink escapes | filesystem | SECURITY-PATH-001 | `tests/test_storage_paths.py` | `tests/full/test_storage_path_corruption.py` | Verified | N/A |
| Job deletion is conflict-safe and has one guarded mutation boundary | database/service | DB-MUTATION-001 | `tests/services/test_job_deletion_service.py` | `tests/full/test_job_deletion_races.py` | Verified | N/A |
| Artifact blobs are reclaimed only at zero artifact_refs past the in-flight grace window; the orphan GC covers zero-ref rows job-deletion GC cannot see, scans the catalog keyset-paginated, and delegates deletion to `delete_unreferenced`'s transactional refcount + grace re-check, driven hourly by the sweeper-owning replica or manually via `scripts/gc_artifacts.py` | database/service | DB-ARTIFACT-GC-001 | `tests/services/test_artifact_orphan_gc.py`, `tests/services/test_artifact_store.py` | N/A | Verified | N/A |
| Continue/resume accepts only resumable paused states and never reopens terminal jobs | service/API | EXEC-STATE-001 | `tests/test_workspace_job_control_flow.py` | N/A | Verified | N/A |
| Production app and router code must not reintroduce legacy video route modules after migration | route | BOUNDARY-VIDEO-002 | `scripts/check_architecture.py`, `tests/scripts/test_architecture_video_legacy.py` | N/A | Verified | N/A |
| Any future `server/app/video_capabilities` module must not import orchestration or runtime modules (reintroduction tripwire; the package was removed with the business extraction) | video_capabilities | BOUNDARY-VIDEO-001 | `scripts/check_architecture.py`, `tests/scripts/test_architecture_video_legacy.py` | N/A | Verified | N/A |
| New Workspace code must not import retired legacy module trees (`server.app.pipeline.*`) | workspace/executor | BOUNDARY-VIDEO-003 | `scripts/check_architecture.py`, `tests/scripts/test_architecture_phase6.py`, `tests/scripts/test_architecture_video_legacy.py` | N/A | Verified | N/A |
| Workspace runtime stats report the single implicit code pool (instance code_capacity, per-workspace/global active leases) without executor configuration tables (P-0.5) | executor/service | EXEC-STATS-001 | `tests/test_workspace_configuration_service.py::test_code_pool_stats_report_capacity_and_leases`, `tests/test_workspace_configuration_service.py::test_code_pool_stats_does_not_consult_agent_status_manager`, `tests/routes/jobs/test_workspace_stats.py::test_workspace_stats_code_pool_reflects_leases` | N/A | Verified | N/A |
| PostgreSQL connections are pooled and deterministically returned | database | DB-LIFECYCLE-001 | `tests/db/test_postgres_runtime.py` | N/A | Verified | N/A |
| Runtime queues and job event sequence numbers are persisted in PostgreSQL; no new in-process authoritative runtime state | database | DB-STATE-001 | `tests/services/test_agent_broker.py`, `tests/events/test_job_event_buffer_db.py`, `tests/ci/test_postgres_only_runtime.py` | N/A | Verified | N/A |
| Failed node runs persist a failure_category restricted to {'', technical, business, unknown}; unrecognized failures classify as unknown, never defaulting to technical | jobs/database | DB-FAILURE-001 | `tests/services/test_failure_classification.py`, `tests/executors/leases/test_failure_classification_persistence.py` | N/A | Verified | N/A |
| Transient failures (db_pool_timeout) return the job_node to pending at the lease finish path for up to 3 attempts instead of failing the job; every attempt stays recorded as a failed node_run | jobs/database | DB-FAILURE-002 | `tests/services/test_failure_classification.py`, `tests/executors/leases/test_failure_classification_persistence.py` | N/A | Verified | N/A |
| The implicit code pool capacity (instance code_capacity) is shared across workspaces and never exceeded | executor | EXEC-CAPACITY-001 | `tests/test_workflow_worker_concurrency.py::test_global_capacity_enforced_by_lease_transaction`, `tests/executors/leases/test_capacity_limits.py`, `tests/workers/test_workflow_worker_capacity.py` | `tests/full/test_executor_worker_fairness.py::test_shared_capacity_and_round_robin_fairness` | Verified | N/A |
| Runnable workspaces receive bounded scheduling opportunity under contention (round-robin only; per-workspace capacity isolation retired with the allocation layer, P-0.5) | scheduler/worker | EXEC-FAIRNESS-001 | `tests/test_workflow_worker_concurrency.py::test_round_robin_allows_small_workspace_to_claim` | `tests/full/test_executor_worker_fairness.py::test_shared_capacity_and_round_robin_fairness` | Verified | N/A |
| Every non-Agent-routed node joins the implicit code pool: no executor binding/allocation concept (schema v47 dropped both tables), capacity from instance code_capacity, per-node limits via workspace_node_limits, lease rows carry the constant 'code' executor id | executor | EXEC-CODE-POOL-001 | `tests/executors/leases/test_capacity_limits.py::test_claims_write_the_code_pool_executor_id`, `tests/workers/test_workflow_worker_thread_local.py::test_worker_creates_the_single_code_pool`, `tests/db/test_executor_retirement_migration.py` | `tests/full/test_executor_cancellation_recovery.py` | Verified | N/A |
| Lease loss and shutdown produce bounded adapter-specific cancellation without leaking executor capacity | executor/worker | EXEC-CANCEL-001 | `tests/executors/test_executor_cancellation.py` | `tests/full/test_executor_cancellation_recovery.py` | Verified | N/A |
| PostgreSQL schema initialization is transactional, advisory-locked, and idempotent | database/migration | RECOVERY-MIGRATION-001 | `tests/db/test_postgres_runtime.py::test_schema_initialization_is_idempotent` | `tests/full/test_agent_worker_control_plane.py::test_postgres_agent_schema_initialization_is_idempotent` | Verified | N/A |
| Secrets use environment overrides and enabled runtime dependencies fail validation at startup | config | CONFIG-STARTUP-001 | `tests/test_settings.py`, `tests/test_startup_validation.py` | N/A | Verified | N/A |
| Worker Service keeps secrets out of its local UI/API, preserves hidden runtime settings, publishes its control port on host loopback by default, and guards every endpoint except /api/health with the local control token | worker/config | CONFIG-WORKER-001 | `tests/workers/test_agent_worker_service.py` | N/A | Verified | N/A |
| Workspace job lifecycle SSE events are broadcast only after the underlying transaction commits | events/service | EXEC-EVENT-001 | `tests/events/test_job_events.py` | `tests/full/test_workspace_sse.py` | Verified | N/A |
| Source file line budgets are enforced and temporary overruns are tracked | architecture | CONFIG-ARCH-001 | `scripts/check_architecture.py`, `tests/scripts/test_check_architecture.py` | N/A | Verified | N/A |
| Agent Nodes enter running only after a compatible Worker atomically claims capacity in both the Workspace and Worker domains | agent/worker | EXEC-AGENTCLAIM-001 | `tests/services/test_agent_broker.py`, `tests/routes/test_agent_workers.py` | `tests/full/test_agent_worker_control_plane.py::test_agent_capacity_matrix_across_workers` | Verified | N/A |
| Agent definitions own capability, runtime, skill, tools, and requirements while Workers declare only runtime compatibility and machine capacity | agent/catalog | EXEC-AGENTDEF-001 | `tests/services/test_agent_catalog.py`, `tests/services/test_agent_broker.py::test_worker_registration_declares_runtime_and_machine_capacity` | `tests/full/test_agent_worker_control_plane.py::test_agent_definition_catalog_snapshot_lifecycle` | Verified | N/A |
| Agent Workers claim executions only for workspaces in their server-side registration scope (global or scoped register token) | security/worker | EXEC-WORKERACL-001 | `tests/services/test_agent_broker.py`, `tests/routes/test_agent_workers.py` | `tests/full/test_agent_worker_control_plane.py::test_scoped_register_token_lifecycle` | Verified | N/A |
| Agent execution capacity counts only `claimed` requests; release-slot flips a finished process to `reporting` (slot freed immediately) while heartbeat/mark_done/sweeper/one-active-node uniqueness keep the result owned until the report commits | agent/worker | EXEC-REPORTING-001 | `tests/services/test_agent_broker.py`, `tests/routes/test_agent_workers.py::test_release_slot_requires_and_validates_lease_id`, `tests/workers/test_agent_worker.py`, `tests/workers/test_worker_upload_queue.py` | N/A | Verified | N/A |
| A node run completes only after the skill's deterministic `validate_output.py` passes: the Host validates unpacked Worker results server-side (Worker-reported success is untrusted); validation failure fails the run (review rejections classify as business/review_rejected) | executor/worker | EXEC-VALIDATION-001 | `tests/services/test_agent_completion_validation.py` | N/A | Verified | N/A |
| The capability `path` binding to repo-tracked node files is retired (#96): no code-execution parameter carrier ever carries a path key (the pydantic model rejects it), the repo-file loader and bare child are gone, and the only repo-tracked node code is the demo workflow's two git-reviewed seed sources under `workflow_nodes/`, copied into workspace-scoped versions when a demo workspace is initialized; startup compatibility migration copies active legacy-global demo code into bound workspaces and archives the global rows; executor definitions themselves retired at schema v47 (P-0.5) | executor/config | EXEC-CODE-001 | `tests/executors/test_code_executor.py`, `tests/services/test_demo_node_seed.py`, `tests/workers/test_workflow_worker_thread_local.py::test_make_workflow_worker_runs_demo_intake_local_node` | N/A | Verified | N/A |
| Node code lives only in the DB with immutable versions and takes effect via the publish flow (draft → published → archived, at most one published per (workspace, node)); all active code is workspace-scoped and ordinary dispatch resolves only the workspace's published version; legacy global rows are migration input and exact-version/hash historical replay data only; no runtime path writes repo `workflow_nodes/` files; since #115 the intake `node_code_versions` freeze and revision `node_code_pins` snapshot are audit records and replay pin sources honored only by quality-replay batches, and the gate `workflows.custom_nodes_enabled` short-circuits every read | executor/config | EXEC-CODE-002 | `tests/db/test_custom_node_codes_migration.py`, `tests/services/test_node_codes.py`, `tests/workers/test_workflow_worker_node_code.py`, `tests/executors/test_code_executor.py::test_execute_without_node_code_is_a_config_error`, `tests/test_workflow_revisions.py::test_publish_revision_records_node_code_pins` | N/A | Verified | N/A |
| All (DB-backed) node code executes only inside the velites OS sandbox (`velites sandbox wrap`: seatbelt/bubblewrap) with deny-by-default policy — read-only fs except job_dir/tmp, network denied unless the capability opts in (`sandbox_network`) — and fail-closed: no sandbox backend means node code never runs, on the Host (the bare multiprocessing child retired with the path mechanism, #96) and on a Worker alike, with code_hash verified before spawn and a startup preflight that refuses code capacity without velites | executor/harness | EXEC-CODE-003 | `tests/executors/test_code_executor.py::test_node_code_fails_closed_without_sandbox`, `tests/executors/test_code_executor.py::test_custom_sandbox_denies_network_by_default`, `tests/executors/test_code_executor.py::test_custom_sandbox_denies_writes_outside_job_dir`, `tests/workers/test_code_runner.py::test_build_sandbox_argv_structure`, `tests/workers/test_runtime_preflight.py::test_preflight_code_capacity_requires_velites`, `velites/tests/sandbox_wrap.rs`（Rust 侧 wrap 策略单测/集成） | N/A | Verified | N/A |
| Code node runtimes never carry a database handle or DSN: builtin and sandboxed children share one runtime key set with DB-derived inputs (job_batch, skill_versions) prefetched by the parent; privileged actions (connection-token invalidation) stay in the parent — nodes only drop an auth-failure marker under job_dir via the node SDK (`NodeContext.report_auth_failure`) | executor/harness | EXEC-CODE-004 | `tests/executors/test_code_executor.py::test_build_runtime_prefetches_inputs_and_hides_db`, `tests/executors/test_code_executor.py::test_sandboxed_child_auth_failure_marker_reaches_parent`, `tests/executors/test_code_executor.py::test_sandboxed_custom_node_can_use_node_sdk`, `tests/workflow_nodes/test_node_sdk.py` | N/A | Verified | N/A |
| The queued kind='code' manifest persists only a lightweight `runtime_context` audit stub (job/workspace ids + `batch_id`/`batch_hash`); the full DB-derived payloads (job, workspace, intake batch, skill_versions) are rebuilt on the claim-response path in memory — never persisted — and code rows reaching a terminal state slim back to the stub automatically, so `agent_execution_requests` never retains the ~1.7MB batch payload (issue #142) | executor/data | EXEC-CODE-MANIFEST-001 | `tests/services/test_code_dispatch.py`, `tests/services/test_code_manifest_trim.py`, `tests/routes/test_agent_workers.py::test_code_claim_injects_secrets_into_response_manifest` | N/A | Verified | N/A |
| Code node executions reach a Worker only through the kind='code' lane (protocol v2): a separate max_code_concurrency pool is accounted and enforced independently of the agent pool, the manifest ships node code text + sha256 code_hash (Worker refuses mismatch) with a workspace_libs snapshot in the bundle, worker-eligibility is a static import-closure scan (⊆ workspace_libs + stdlib + requests, cached by code_hash) and the Host-only exemption list is empty (`EXEMPT_NODES = ()`), so all in-repo nodes are worker-eligible; dispatch falls back to the local Host executor when no online code-capable Worker exists, with the online probe mirroring the claim-side filters (capability, protocol v2, allowed_workspaces admission) so a Worker that could never claim does not count as online (#125 added a JIT stock gate before enqueue) | executor/worker | EXEC-CODE-WORKER-001 | `tests/services/test_code_claim.py`, `tests/services/test_agent_broker_claim_windows.py`, `tests/workers/test_code_stock_gate.py`, `tests/services/test_code_dispatch.py`, `tests/workflow_nodes/test_node_self_containment.py`, `tests/workers/test_code_runner.py::test_execute_code_hash_mismatch_refuses_before_spawn`, `tests/db/test_agent_request_kind_schema.py`, `tests/routes/test_agent_workers.py` | N/A | Verified | N/A |
| Agent Worker credentials are per-worker and revocable; only sha256 hashes of token secrets are persisted | security/worker | SECURITY-WORKER-001 | `tests/routes/test_agent_workers.py`, `tests/services/test_agent_broker.py::test_worker_registration_declares_runtime_and_machine_capacity` | `tests/full/test_agent_worker_control_plane.py::test_worker_token_is_hashed_and_revocable` | Verified | N/A |
| Shard executions are claimed through ExecutorLeaseRepository.try_claim with one lease per shard; fan-out never bypasses the capacity system | executor/scheduler | EXEC-SHARD-001 | `tests/workflows/test_sharding.py` | `tests/full/test_shard_fanout_e2e.py` | Verified | N/A |
| User sessions are revocable bearer tokens persisted only as sha256 digests; management endpoints require admin; business routes require an authenticated user; workspace membership gates viewer reads and editor writes; repeated login failures lock the account | security/auth | SECURITY-AUTH-001 | `tests/routes/test_auth_routes.py`, `tests/routes/test_auth_matrix.py`, `tests/db/test_auth_queries.py` | `tests/full/test_auth_control_plane.py` | Verified | N/A |
| Studio agent runs authenticate with short-lived scoped tokens (sha256 at rest, bound to the initiating user + actor_scope=studio_agent, TTL sliding forward at Studio chat turn start when close to expiry, revocable, Bearer-only; run tokens additionally bind the chat session's workspace, schema v45); every effecting endpoint mounts reject_studio_agent_scope and refuses scoped tokens with 403 while draft/validate endpoints stay reachable — effecting endpoints cover the publish/rollback/archive flows (workflow drafts, node code, agent definitions — executor definition endpoints retired at v47), job lifecycle and execution triggers (delete, rerun/run-to/continue and batch variants, batch intake, workflow upgrade, quality replays), workspace/secret/package/member/settings/configuration writes, materials upload lifecycle (presign/complete/delete), and worker pause/resume — and require_admin refuses any scoped identity outright so a scoped token inheriting role=admin still cannot reach admin endpoints | security/auth | STUDIO-AGENT-001 | `tests/routes/test_studio_agent_scope.py`, `tests/routes/test_studio_agent_tools.py`, `tests/routes/test_studio_agent_context.py`, `tests/services/test_scoped_tokens.py`, `tests/db/test_auth_scoped_tokens_migration.py` | N/A | Verified | N/A |
| Studio chat ACP agent command lines come only from the admin-maintained instance registry (non-admin picker API never exposes command/args and only lists agents whose command resolves on this host via a cached PATH-level probe; creating a session with an unavailable agent fails before any spawn); each session mints one scoped token (origin=run, TTL renewed at turn start per STUDIO-AGENT-001) that crosses the process boundary only as an `Authorization: Bearer` header to the in-app streamable-HTTP MCP endpoint `/api/studio-agent/mcp`, with the chat-session binding riding the `x-agent-legion-mcp-session-id` header (`server/app/mcp_server/http_app.py`, `config.py:30`), and is revoked on close; agent-legion MCP tool permissions auto-approve, all other tool calls default to human confirmation with a per-session allow-all switch, and cancel settles pending permissions as denied | security/auth | STUDIO-AGENT-002 | `tests/routes/test_studio_chat.py`, `tests/routes/test_studio_agents_admin.py`, `tests/services/test_studio_chat_service.py`, `tests/services/test_studio_chat_availability.py`, `tests/db/test_studio_chat_schema.py`, `tests/mcp_server/test_http_app.py` | N/A | Verified | N/A |
| Agent manifests carry only config_schema-whitelisted, non-secret config keys; effective node config resolves schema defaults → node config → workspace override and is frozen into the intake batch payload; schema source priority is Agent Definition → node-declared `config_schema` (the executor capability fallback retired with the executor concept, P-0.5/schema v47), and code-routed nodes get the reserved execution keys `timeout_seconds`/`sandbox_network` auto-merged (v47 harvest moved executor-level values onto revision nodes' config; old frozen batches padded underneath at dispatch) | security/config | CONFIG-MANIFEST-001 | `tests/test_config_schema.py`, `tests/services/test_node_config.py`, `tests/test_workflow_definition.py` | `tests/full/test_node_config_manifest.py` | Verified | N/A |
| Node config keys are intake-frozen by default; a config_schema property declared `runtime_mutable: true` is re-resolved at every dispatch along the same chain (defaults → node config → workspace override) so run switches apply to the next node execution of an in-flight job, while the reserved execution keys always stay frozen; the non-secret resolved config used per run is audited onto `node_runs.config_snapshot_json` (schema v49; lease claim for the local code pool, enqueue-time manifest config for the Worker/Agent paths) | security/config | CONFIG-RUNTIME-MUTABLE-001 | `tests/test_config_schema.py`, `tests/services/test_node_config.py`, `tests/executors/test_leases.py`, `tests/services/test_code_claim.py` | N/A | Verified | N/A |
| Workspace secrets are Fernet-encrypted at rest, stored/frozen only as `secret_ref` markers, write-only over the API, resolved in memory at server-side consumption points, and covered by log redaction; the code→Worker path keeps the same boundary across machines — queued manifests/bundles are stored secret-free, plaintext is resolved and injected only into the claim response over HTTPS, and the Worker holds secrets in memory only (stdin to the child; secret-marked keys never reach the Worker — the Host-side split_manifest_config keeps them out of the dispatch manifest) | security/vault | VAULT-SECRET-001 | `tests/services/test_vault.py`, `tests/routes/test_workspace_secrets.py`, `tests/services/test_code_dispatch.py::test_enqueue_builds_secret_free_manifest_and_bundle`, `tests/services/test_code_dispatch.py::test_resolve_code_manifest_config_injects_and_strips_secrets`, `tests/workers/test_code_runner.py::test_secrets_stay_off_disk` | `tests/full/test_vault_secret_freeze.py` | Verified | N/A |
| External service credentials are instance-level and admin-managed: connections store non-sensitive config only (secret fields diverted into the instance vault as `conn:<key>:<field>` refs), acquired tokens are cached Fernet-encrypted with single-flight refresh under the parent row lock, auth-protocol semantics live in workspace packs behind the platform adapter registry, and connection config + tokens are injected into node runtimes in memory only at dispatch — never persisted, never in agent manifests | security/connections | SECURITY-EXTERNAL-CONNECTION-001 | `tests/services/test_connections.py`, `tests/services/test_connection_tokens.py`, `tests/routes/test_connections_api.py` | N/A | Verified | N/A |
| Tracked split config yaml files carry no secret values: the global `cms:` section is retired from split yaml (CMS selector defaults live in the capability config_schema; endpoint URLs and credentials live on the instance-level external connection), so a `cms` key in any split file fails the owned-key check, while single-file explicit configs still reject retired `cms.token` / `cms.token_gen` keys (G2) at startup; the `openclaw` section is retired from split yaml into the DB instance settings document (a split-file `openclaw` key fails the owned-key check), and `openclaw.skill_safety` refs (G3, pinned by the DB `skill_lock` document) are rejected at startup validation and by the instance-settings admin API (422); the env-only sections `vault` / `auth` are rejected by the owned-key check in any split file; secrets reach the runtime only via env overrides or the vault | security/config | CONFIG-YAML-001 | `tests/test_config_yaml_hygiene.py`, `tests/test_settings.py`, `tests/test_configuration_loader.py` | N/A | Verified | N/A |
| velites event stream schema stays wire-compatible with the Host consumers (allowlist, usage/error/tool fields, retry and outputs_validation payloads) and never emits delta events | executor/events | EXEC-EVENT-SCHEMA-001 | `tests/executors/test_velites_event_contract.py` | N/A | Verified | N/A |
| Material object-storage configuration is platform infrastructure injected env-only (`AGENT_LEGION_S3_*` with `_FILE` variants for the access/secret keys, same pattern as the vault master key): credentials never land in tracked yaml, the database, API payloads, or logs; an unconfigured instance (no bucket) keeps every other feature up and degrades only the materials API to 503 | security/storage | MATERIAL-SECRET-001 | `tests/services/test_s3_settings.py`, `tests/routes/test_materials_routes.py` | N/A | Verified | N/A |
| Node code accesses materials only through the materialization cache (materials-and-runs design §6.2): the dispatching parent (Host `build_runtime` / Worker `code_runner`) downloads the object into a content-addressed local cache (`shared/material_cache.py`, atomic tmp+rename, oldest-mtime eviction towards a byte budget) and both sandbox argv builders grant a single static allow-read root for that cache directory — dynamically allow-reading arbitrary host paths is forbidden; the Worker receives a claim-time presigned GET URL (memory-only, like secret injection) and needs no storage credentials; the node SDK exposes only the materialized local path (`ctx.material`) | executor/materials | MATERIAL-ACCESS-001 | `tests/executors/test_material_dispatch.py`, `tests/workers/test_material_fetch.py`, `tests/shared/test_material_cache.py` | N/A | Verified | N/A |
| A bundle is a folder uploaded as ONE run item (materials-and-runs design §5, #156): members upload through the regular materials channel and one create call freezes an immutable reference-only manifest (`material_bundles` + `material_bundle_members`, schema v55); deletion is guarded both directions (member-of-bundle materials and run-referenced bundles refuse delete), dispatch materializes the bundle as a hardlink directory tree at a deterministic bundle address shared by Host and Worker (`shared/material_bundle.py`), Worker fetch reuses the claim-injected presigned channel, and the item/contract type is the single literal `bundle` — the start-node DEFAULT contract stays `('material','ref')` so legacy workspaces reject bundle items fail-closed | executor/materials | MATERIAL-BUNDLE-001 | `tests/services/test_material_bundles.py`, `tests/services/test_bundle_materialize.py`, `tests/routes/test_material_bundles_api.py`, `tests/routes/test_runs_bundle_api.py`, `tests/shared/test_material_bundle.py`, `tests/shared/test_material_cache.py`, `tests/services/test_material_ttl.py`, `tests/workers/test_bundle_fetch.py` | N/A | Verified | N/A |
| Frozen inputs, node configs and pins are authoritative on the run/job columns (`runs.frozen_pins_json`, `jobs.input_json`, `jobs.frozen_config_json`): dispatch and the node SDK rebuild the legacy batch-payload shape from those columns via `server.app.services.run_payload`, and no runtime path reads a stored batch payload (`job_batches` is dropped at schema v53; the async intake queue keeps only its working state in `runs.queue_payload_json`) | executor/config | RUN-FREEZE-001 | `tests/db/test_runs_migration.py`, `tests/services/test_run_payload.py` | N/A | Verified | N/A |
| Every workflow definition carries exactly one `type: start` node holding the entry contract `accepted_item_types` (loader auto-injects a synthetic accept-all start pointing at the implicit roots when absent, so legacy definitions keep their readiness behavior); the start node never executes — excluded from `job_nodes` seeds, node-config resolution, publish code-resolution checks and `allowed_nodes`, and treated as definitionally completed by the scheduler — and `RunService.create_run` rejects items of unaccepted types before any write | executor/workflow | EXEC-WORKFLOW-START-001 | `tests/workflows/test_workflow_start_node.py`, `tests/test_workflow_scheduler.py::test_legacy_snapshot_without_start_keeps_readiness_behavior`, `tests/services/test_run_service.py::test_ref_item_rejected_under_material_only_contract`, `tests/routes/test_runs_api.py::test_item_type_rejected_by_start_contract` | N/A | Verified | N/A |
| velites has zero auto-discovery: context comes only from --system-prompt, --skill, and the @prompt instruction; files in the working directory (e.g. AGENTS.md) never reach provider requests or the event stream | executor/harness | EXEC-HARNESS-ISOLATION-001 | `tests/executors/test_velites_controllability.py::test_no_auto_discovery` | N/A | Verified | N/A |
| velites run budgets (--max-turns, --max-tokens, wall-clock deadline) are enforced in-loop with one wrap-up turn and always terminate with agent_end reason=budget_exceeded | executor/harness | EXEC-HARNESS-BUDGET-001 | `tests/executors/test_velites_controllability.py::test_budget_exhaustion_marks_agent_end` | N/A | Verified | N/A |
| velites enforces the declared output contract at exit: --require-output artifacts still missing when a non-cancelled run ends (normal stop, budget exhaustion, unrecovered model error) make the harness exit 1 — no exit-0 run with missing declared outputs reaches the caller; cancellation is exempt | executor/harness | EXEC-HARNESS-OUTPUTS-001 | `tests/executors/test_velites_controllability.py::test_require_output_still_missing_reports_validation` | N/A | Verified | N/A |
| The skills base dir is a read-only runtime input: cache lock files live under runs_dir, and resolving a clean pinned cache performs no writes — a read-only skills mount works for steady-state executions and validations; writes only on cache miss/version change | executor/skills | EXEC-SKILL-HERMETIC-001 | `tests/test_skill_manager.py::test_get_skill_dir_works_with_read_only_skills_base`, `tests/test_skill_manager.py::test_cache_lock_files_live_under_runs_dir` | N/A | Verified | N/A |
| The skills runs dir is OS-managed scratch, never a sibling of the shared skills base dir: the default is a deterministic per-user temp dir overridable via `AGENT_LEGION_SKILLS_RUNS_DIR` and resolved identically by every process sharing the skill cache (FileLock domain), and leaked execution snapshots are reclaimed by the sweeper-thread GC (mtime > 1h; `.locks`, non-directories and symlinks untouched) with the OS temp TTL as backstop | executor/skills | EXEC-SKILL-RUNS-SCRATCH-001 | `tests/skills/test_runs_dir_location.py`, `tests/executors/test_sweeper_startup.py::test_periodic_sweep_runs_skill_execution_gc` | N/A | Verified | N/A |
| Skill cache cleanliness probes (rev-parse/status) are memoized per cache dir for the doc-cache TTL: dirt is served as-is within the TTL and repaired by checkout+clean on the first probe after expiry; a commit change always re-probes; ttl=0 restores per-call probing | executor/skills | EXEC-SKILL-CLEAN-TTL-001 | `tests/test_skill_manager.py::test_cleanliness_probes_memoized_within_ttl`, `tests/test_skill_manager.py::test_cleanliness_probes_rerun_after_ttl_and_repair_dirty_cache`, `tests/test_skill_manager.py::test_cleanliness_probes_every_call_when_ttl_zero` | N/A | Verified | N/A |
| velites confines the bash tool in a default-on OS-level filesystem sandbox (macOS seatbelt / Linux bubblewrap): reads limited to cwd/session dir/--skill dirs/system paths, writes limited to cwd/session dir/$TMPDIR//tmp; unavailable backend fails closed, --no-sandbox is the only escape hatch | executor/harness | EXEC-HARNESS-SANDBOX-001 | `tests/executors/test_velites_sandbox.py` | N/A | Verified | N/A |
| The velites bash tool rejects full-disk scan commands (recursive traversal from broad roots such as `find /`) before spawn, with or without the OS sandbox, returning remediation guidance to the model; scoped searches still run | executor/harness | EXEC-HARNESS-GUARD-001 | `tests/executors/test_velites_bash_guard.py` | N/A | Verified | N/A |
| Agent command construction is pinned by `AgentDefinition.runtime`: pi builds the pi argv, velites builds the velites argv; provider/model/thinking resolve strictly from node override then workspace default, provider identity passes unchanged into the runtime, and missing/unknown values fail fast | executor/dispatch | EXEC-RUNTIME-DISPATCH-001 | `tests/workflows/test_velites_command.py`, `tests/services/test_agent_broker.py::test_dispatch_fails_fast_on_unsupported_runtime` | `tests/full/test_velites_harness_e2e.py::test_velites_runtime_agent_worker_chain_end_to_end` | Verified | N/A |
| Worker model capability is runtime-owned and fail-closed: each selected runtime is discovered through its adapter, intersected with its runtime-scoped local allowlist, and registered as protocol-v3 runtime/provider/model triples; one failed runtime advertises no models without disabling the others or the code pool; velites discovery comes exclusively from `~/.velites/models.json` | agent/worker | EXEC-RUNTIME-MODELS-001 | `tests/workers/test_runtime_models.py`, `tests/services/test_agent_claim_compatibility.py`, `velites/tests/models_cli.rs` | N/A | Verified | N/A |
| DB path columns (node_runs.log_path/run_dir/session_dir, jobs.storage_dir) store data-dir-relative paths only; all write boundaries canonicalize, legacy absolute manifest log_path heals at agent claim without crashing the claim scan, a startup report surfaces remaining absolute rows and runs on a background thread so readiness never blocks on the scan (issue #106), reads stay fail-closed | executor/storage | EXEC-PATH-STORAGE-001 | `tests/services/test_agent_broker_claim_scan.py::test_claim_heals_legacy_absolute_log_path`, `tests/services/test_path_hygiene.py` | N/A | Verified | N/A |
| Job storage directories use the sharded layout `jobs/<workspace>/<sha1(job_id)[:2]>/<job_id>`: new dirs are created via `storage_layout.job_storage_dir` only, legacy flat dirs stay readable through the stored `jobs.storage_dir` and dual-probe fallbacks, and maintenance sweeps locate job dirs from the jobs table instead of enumerating workspace directories | executor/storage | EXEC-PATH-STORAGE-002 | `tests/services/test_job_storage_layout.py`, `tests/test_run_dir_cleanup.py` | N/A | Verified | N/A |
| A queued Agent request no single non-revoked Worker can claim (per-Worker judgment on runtime + capability + model, never cross-Worker unions) is failed by the unclaimable sweeper with an explicit reason; it never rots in queued (zero-Worker deployments stay untouched) | agent/worker | EXEC-CLAIM-RUNTIME-001 | `tests/services/test_agent_broker_concurrency.py::test_unclaimable_sweeper_fails_on_runtime_mismatch`, `tests/services/test_agent_broker_concurrency.py::test_unclaimable_sweeper_fails_on_cross_worker_combination`, `tests/services/test_agent_broker.py::test_pi_only_worker_cannot_claim_velites_request` | N/A | Verified | N/A |
| A quality replay never touches the original job: it builds an isolated copy job from the frozen workflow snapshot (upstream completed, downstream not_applicable, only the replayed node scheduled), file-copies frozen inputs, and dispatches the pinned immutable Agent version (draft/published/archived) via the frozen batch payload pin; the broker enqueue check, claim candidate join, and definition sweepers match the pinned version row instead of published | agent/dispatch | EXEC-QUALITY-REPLAY-001 | `tests/services/test_quality_replays.py`, `tests/services/test_agent_version_pin.py`, `tests/routes/test_quality_replay_routes.py` | N/A | Verified | N/A |
| jobs.updated_at is the workflow worker's scheduling change signal: every evaluation-relevant jobs-row mutation bumps it in the same transaction, the poll pass delta-refreshes its mark cache off a database-clock-sliding watermark (overlap window covers out-of-order commits without pinning to a bulk-enqueue burst), a periodic full rescan reconciles deletions and long-transaction misses, and running jobs are always re-evaluated (covering pending shards) | workflow/scheduler | DB-SCAN-INCREMENTAL-001 | `tests/workers/test_workflow_worker_mark_scan.py`, `tests/workers/test_workflow_worker_ready_queue.py` | N/A | Verified | N/A |
| count_jobs_by_status reads the trigger-maintained workspace_job_status_counts table (O(rows of statuses) instead of an O(workspace jobs) group-by on every aggregator flush / list snapshot / intake / deletion); row triggers on jobs keep it transactionally in sync across all write paths, the v36 backfill rebuilds it idempotently, and the read preserves the legacy queued → pending merge with zero-count statuses omitted | database/service | DB-JOB-STATUS-COUNTS-001 | `tests/db/test_job_status_counts_migration.py` | N/A | Verified | N/A |
| count_workspace_job_nodes_by_status reads the trigger-maintained workspace_job_node_status_counts table (schema v56, #121) instead of an O(workspace job_nodes) job_nodes ⋈ jobs group-by per DAG-endpoint call (48s at 260k jobs / 2.9M job_nodes); per-row job_nodes triggers derive workspace_id/workflow_key from the parent jobs row, a set-based jobs BEFORE DELETE trigger covers cascade deletes, a set-based AFTER UPDATE OF workspace_id/workflow_key trigger re-keys counts, the v56 backfill rebuilds idempotently, and the read omits zero-count rows so results match the group-by it replaced | database/service | DB-JOB-NODE-STATUS-COUNTS-001 | `tests/db/test_job_node_status_counts_migration.py` | N/A | Verified | N/A |
| The global workflow catalog (workflow_catalog table, schema v40) is retired at schema v50 (#112): a workflow is the DAG inside one workspace — default_workflow_key is a plain text identifier, the workspace's ACTIVE revision is the only runtime definition source (node override validation, settings schemas, snapshot-less job fallbacks, package artifact curation, and the worker scan list all read it), the list/register APIs and studio-agent register/list tools are gone; schema v62 (#211) supersedes the creation semantics — workspace creation takes an explicit id that IS the workflow key (immutable, no create-path seeding, demo provisioning via `make import-demo`), and the first-publish key adoption path is removed | workflow/config | DB-WORKFLOW-CATALOG-001 | `tests/db/test_workflow_catalog_retirement.py`, `tests/workers/test_workspace_scan.py`, `tests/routes/test_studio_agent_tools.py` | N/A | Verified | N/A |
| Workspace id and workflow key are one identifier (schema v62, #211): POST /api/workspaces requires an explicit id (pattern ^[a-z0-9][a-z0-9_-]{0,63}$) bound to default_workflow_key at creation and immutable afterwards — PATCH and PUT /configuration reject key changes, the publish guard rejects mismatched draft keys with 422, the create path seeds nothing (demo provisioning is `make import-demo` / scripts/seed_demo.py with the demo workspace id pinned to its key), and the v62 migration renames legacy ids to their keys (cascading workspace_id through every child table plus the FK-less auth_scoped_tokens and ops_metric_samples, fail-fast on conflicts) while backfilling never-published workspaces with key = id; default_workflow_key is deprecated pending full retirement (#211) | database/config | DB-WORKSPACE-KEY-BINDING-001 | `tests/db/test_workspace_id_key_binding.py`, `tests/routes/jobs/test_workspace_crud.py`, `tests/test_workspace_settings_api.py` | N/A | Verified | N/A |
| The durable copy of every declared job artifact lives in the instance object store under `jobs/{workspace_id}/{job_id}/{name}` with `job_artifacts` (schema v54, #160) as the authoritative manifest; the local job_dir is execution staging plus an evictable cache — eviction (`AGENT_LEGION_JOB_CACHE_MAX_BYTES`) only unlinks files whose manifest row exists, size matches, and content_hash (when recorded) re-verifies against the local sha256, re-checking before each job's first unlink that the job is still completed with no active lease; read paths resolve local-first with an object-storage fallback (quality artifact_contents is a deliberate manifest-first exception) so legacy jobs keep working without data migration | executor/storage | EXEC-ARTIFACT-STORE-001 | `tests/services/test_agent_completion_remote.py`, `tests/db/test_job_artifacts_schema.py`, `tests/services/test_job_artifact_maintenance.py`, `tests/routes/test_agent_claim_artifact_channel.py` | N/A | Verified | N/A |
| Worker artifact transfer shares the materials S3 channel instead of a dedicated return protocol: the Host injects presigned PUT specs (`artifact_uploads`) and presigned GET input refs into the claim manifest (presigned URLs are memory-only, expiry derived from node timeout as max(3600, timeout + 900), never logged/persisted), the Worker streams outputs straight to the object store and reports `{storage_key, size_bytes, content_hash}` refs, and the Host HEAD-verifies every ref (including a max_archive_bytes size cap; cancelled results' partial artifacts get a streaming sha256 re-verification) before recording it and staging the bytes into the job_dir; both directions stay compatible with the legacy content-addressed `/api/artifacts` protocol | agent/worker | EXEC-ARTIFACT-WORKER-001 | `tests/workers/test_artifact_object_channel.py`, `tests/services/test_agent_artifact_inject.py`, `tests/routes/test_agent_worker_result_metadata.py` | N/A | Verified | N/A |
| Executor adapters are substitutable through typed runtime configuration | executor/config | Behavior-only | `scripts/check_architecture.py`, `tests/executors/test_executor_runtime.py` | N/A | Behavior-only | N/A |
| Worker lease/restart/shutdown preserves capacity accounting | executor/worker | Behavior-only | `tests/executors/test_executor_recovery.py`, `tests/workers/test_workflow_worker_thread_local.py` | N/A | Behavior-only | N/A |
| Node-limit updates are atomic (allocation/binding retired at v47) | workspace/config | Behavior-only | `tests/test_workspace_configuration_service.py` | N/A | Behavior-only | N/A |
| Legacy workspace agent/pipeline paths are retired | legacy | Behavior-only | `tests/scripts/test_architecture_executor_governance.py::test_legacy_module_is_not_importable` | N/A | Behavior-only | N/A |
| Phase 6 workspace UI behavior remains decoupled from backend internals | frontend | Deferred | N/A | N/A | Deferred | Internal Phase 6 workspace capability-experience-parity plan (not published) |
