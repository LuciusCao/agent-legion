exemptions:
- check: architecture.file_budget
  path: scripts/architecture/budget_monotonicity.py
  reason: 'Issue #641 opens the exemption re-file channel: the exemption layer of
    ceiling_regression_errors now admits a re-file within growth_allowance of the floor
    and an arbitrarily high one carrying a future expires date (FrozenCeiling NamedTuple
    plus the banded judgment) — 4 effective lines past the 81 baseline. The NamedTuple
    lives here rather than file_budgets to keep the guard importing its producer only
    one way. Remove when the re-file judgment folds into budget_floor_errors or a
    budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 85
- check: architecture.file_budget
  path: scripts/architecture/file_budgets.py
  reason: 'Issue #641 threads the growth_allowance band and the FrozenCeiling expiry through
    the budget evaluator: _build_frozen_ceilings returns ceiling+expires pairs and
    check_file_budgets passes the policy band into the monotonicity guard — 3 effective
    lines past the 168 baseline. The evaluator is the single place ceilings, exemptions
    and the band intersect. Remove when the frozen-ceiling plumbing moves into
    budget_monotonicity wholesale or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 171
- check: architecture.file_budget
  path: scripts/check_invariants.py
  reason: 'Issue #641 wires the budget policy growth_allowance into validate_exemptions so
    the exemption ceiling check shares the same band as the file-budget gate, and the
    load failure surfaces as an explicit error — 3 effective lines past the 69 baseline.
    The alternative is exempting ceiling drift between the two gates (a misregistered
    exemption would only fail one of them). Remove when the two registry validators merge
    into a single entry point or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 72
- check: architecture.file_budget
  path: scripts/quality/exemptions.py
  reason: 'Issue #641 adds the optional expires deadline to the exemption schema: the field,
    the strict-ISO-date validator with the today-parameterized hard failure on expiry, and
    the growth_allowance parameter that keeps the ceiling-vs-actual check consistent with
    the file-budget gate — 21 effective lines past the 113 baseline. The expiry wording is
    the gate copy developers see, so it lives with the validator. Remove when expiry checks
    move into issue_state expiry governance or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 140
- check: architecture.file_budget
  path: scripts/architecture/budget_policy.py
  reason: 'Issue #641 adds the growth_allowance policy knob: one dataclass field, the
    _parse_growth_allowance loader (optional, non-negative, default 0 keeps the strict
    pre-#641 line) and its _check_keys whitelist entry — 9 effective lines past the 153
    baseline. The guard files are budgeted by their own gate; the alternative (parking
    the parser in a sibling module) would split the typed policy loader in two for a
    nine-line feature. Remove when the allowance parser folds into a shared optional-int
    helper or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 162
- check: architecture.file_budget
  path: velites/src/contract.rs
  reason: 'Issue #542 migrates the machine-readable contract location to the skill-root contract.yaml:
    the parse tiering (root wins completely and fails closed on a malformed file, embedded-block fallback
    stays for the deprecation window) lands in Contract::parse plus three new-tier unit tests — ~116 effective
    lines past the 513 baseline, still under the 800 soft budget. The engine is the single shared implementation
    for the validate tool, the --require-output gate and the validate subcommand (three consumers, one
    module); the fallback tier and the tier tests are inseparable from the parse they pin. Remove when
    the deprecated embedded-block tier retires (planned follow-up once example/instance skills are migrated)
    or the unit tests move to a sister integration module under velites/tests/.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 627
- check: architecture.file_budget
  path: server/app/services/skill_edit_checks.py
  reason: 'Issue #542 adds the strict root contract.yaml validation (PyYAML parse + structure + jsonschema
    compile check) and the missing-contract warning channel to the shared skill-edit checks module — the
    graded format layer (error vs warning) that save_version/create_skill and the validate tool all consume.
    ~123 effective lines past the 61 baseline. The parser is the parity anchor for the velites Contract::parse
    semantics; splitting it out would fork the graded rules from the path-safety helpers the two creation/edit
    flows already share here. Remove when the contract.yaml parser moves into its own module (e.g. a skills/contract_probe
    sibling) or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 192
- check: architecture.file_budget
  path: server/app/workflows/skill_repo_gate.py
  reason: 'Issue #542 adds the advisory publish pass (skill_repo_publish_warnings) beside the error pass;
    the node-to-skill resolution walk was extracted into the shared _agent_node_skills helper so the two
    lists cannot drift — 70 effective lines vs the 50 baseline. The gate is the single publish-time seam
    for skill presence; splitting the warning pass into a sibling module would fork the resolution loop
    it must share. Remove when the publish validation gains a structured multi-pass framework or a budget
    re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 79
- check: architecture.file_budget
  path: server/app/services/skill_creation.py
  reason: 'Issue #542 makes create_skill the strict birth gate: the four-file set check plus the proposed-payload
    contract.yaml strict parse (scratch-dir staging of the proposed text) — 205 vs the 199 baseline. The
    birth gate belongs with the create flow it guards. Remove when a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 205
- check: architecture.file_budget
  path: server/app/services/skill_validator.py
  reason: 'Issue #542 adds the display-only machine-contract tier warnings to the human-side validator
    (embedded-block deprecation nudge / existence-only notice) — 110 vs the 105 baseline. The tier probe
    is one call plus phrasing; the validator stays the single editor-side read model. Remove when a budget
    re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 110
- check: architecture.file_budget
  path: server/app/services/studio_publish_requests.py
  reason: 'Issue #542 computes the read-side publish advisories (skill-contract warnings) at request/status/poll
    time via _draft_warnings and attaches them with with_draft_warnings — 153 vs the 139 baseline. The
    advisory enrichment must sit on the same service methods the routes already call; persisting instead
    would need the schema change #542 explicitly avoids. Remove when the publish-request read model gets
    its planned split or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 153
- check: architecture.file_budget
  path: server/app/routes/studio_agent_skill_contracts.py
  reason: 'Issue #542 adds the warnings channel to the validate/save-version tool responses (SkillValidationWarning
    model + the two response fields) — 35 vs the 30 baseline. The contracts file is the single wire-shape
    anchor for the studio-agent skill tools. Remove when a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 35
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/validation/WorkflowPublishReviewDialog.tsx
  reason: 'Issue #542 adds the optional advisories block (warning-colored list, data-testid publish-advisories)
    to the shared publish review dialog — 87 vs the 74 baseline. The dialog is shared by the manual and
    agent publish flows; the advisory presentation is one guarded block. Remove when a budget re-balance
    re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 87
- check: architecture.file_budget
  path: scripts/architecture/effective_lines.py
  reason: 'Issue #610 adds the docstring-row detection to the Python effective-line metric (ast walk over
    module/class/function first statements plus the own-row boundary check against code sharing the row)
    — ~4 effective lines past the 88 baseline. The metric module IS the single counting authority every
    budget decision reads; splitting the docstring scanner into a sister module would fork the metric
    from the file that gates it. Ceiling at actual + 10 per the dated-exemption convention. Remove when
    the metric module gets its planned split (scanner vs counting) or a budget re-balance pass re-registers
    it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 92
- check: architecture.file_budget
  path: frontend/src/pages/globalSettings/instanceSettingsCapacityFields.ts
  reason: 'Issue #569 adds the result-validate pool knob beside its #554 result_unpack sibling: one field
    entry (path, label, integer, allowZero, max) in the same table shape. Issue #591 adds the result-commit
    batching kill-switch toggle to the same capacity group (the toggle entry body lives in instanceSettingsFieldTypes.ts
    since #612''s re-baseline left every group table at its wall — only the reference registers here).
    The capacity-knob group IS the form surface for the executor-runtime pool sizes; splitting a one-entry
    overflow into a sister module would fork the group. Remove when the instance-settings form gets its
    planned structural split or a knob retirement frees headroom.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 42
- check: architecture.file_budget
  path: frontend/src/pages/globalSettings/instanceSettingsFields.ts
  reason: 'Issue #521 adds the result-commit gate knob to the Worker-limits field group: one field entry
    (path, label, integer, allowZero) in the same table shape. The list IS the form surface.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 122
- check: architecture.file_budget
  path: server/app/agent_broker/broker.py
  reason: 'Issue #591 threads the optional result_batcher seam through mark_done (submit branch + the
    _mark_done_direct rename) plus the constructor parameter — the batched arm itself lives in sister
    module mark_done_batch.py, but the submit hop must sit on the method every caller already reaches
    (the batcher and the direct path share one entry point). Re-sized down from 268 under #612''s docstring-free
    metric (docstring rows no longer count). Ceiling at actual + 10 per the dated-exemption convention
    (#521 leases.py precedent). Remove when the broker gets its planned structural split or the group-commit
    queue retires.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 227
- check: architecture.file_budget
  path: server/app/bootstrap/agent_plane.py
  reason: 'Issue #591 constructs the ResultCommitBatcher before the broker and the lease repository (both
    receive it) and exposes it on the AgentPlane dataclass for the lifespan start/stop — the plane is
    the composition root the wiring must read at. The codex review round (C1) added the result_batching
    gate parameter so a start_worker=False app builds no batcher at all. Re-sized down from 101 under
    #612''s docstring-free metric. Ceiling at actual + 5 per the dated-exemption convention. Remove when
    the plane assembly gets its planned restructure.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 92
- check: architecture.file_budget
  path: server/app/agent_control/completion.py
  reason: 'Issue #521 adds the stage boundary marks (unpack/artifacts_verify/ validate/artifacts_upload)
    inline between the physical steps they separate; each mark is one line at the exact boundary it names.
    The marks retire with the stage split''s phase 2 rework (per-stage slimming restructures finish()
    and re-budgets the module).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 176
- check: architecture.file_budget
  path: server/app/executors/leases.py
  reason: 'Issue #521 threads the optional stage_timer keyword through finish() into _lease_write_paths;
    the parameter must ride the same retry wrapper as the result it instruments (a retry re-runs the stages
    it re-measures).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 126
- check: architecture.file_budget
  path: server/app/services/path_hygiene.py
  reason: 'Issue #521 adds the per-path warn dedupe (set + lock + keyed signature) and the one-time rewrite
    orchestration (rebase rule, chunk loop, two daemon-thread kickers). The rewrite rides the facade queries
    added in jobs/queries/path_hygiene; the service keeps the warnings/report/rewrite story in one module.
    Shrinks back once every deployment''s stored absolute-path rows have been rewritten and the rewrite
    pass can drop.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 104
- check: architecture.file_budget
  path: server/app/routes/runtime_profile_contracts.py
  reason: 'Issue #521 mirrors the fourteen result-stage gauge columns into ProfileBucket, the same field-list
    growth the v78 claim-stage split made. The model IS the response contract.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 55
- check: architecture.file_budget
  path: worker/cli_args.py
  reason: 'The workerctl --proxy flag (#444 rework) gives headless workers (Docker form, console behind
    an ssh tunnel) the same config surface the UI already has: the console path needs a tunnel and hand-editing
    the mounted yaml triggers the supervisor divergence warning without taking effect. Five lines of honest
    cost (one argparse declaration with its help text, one payload mapping entry); the file is a flat
    argv table where each line IS a flag — nothing to split without forking the parser. Remove together
    with the supervisor spawn split (#451).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/451
  ceiling: 94
- check: architecture.file_budget
  path: worker/executor_log.py
  reason: '#510 grew this module by the events split: events_log_path anchoring, the
    is_structured_event sniff (containment-shaped after review R1 — a prefix check
    dropped the forensic events), and PanelLogSinks bundling the two sinks'' lifecycle
    so supervisor._log stays one delegation. Actual 83 vs the 67 floor. Harvest when
    the sink family next restructures (e.g. a third channel or a sink-config module).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 83
- check: architecture.file_budget
  path: worker/supervisor.py
  reason: The proxy config injection (#444) adds the env override dict at the single executor spawn site
    — the one place where os.environ is snapshotted into the child — so proxy semantics (configured proxy
    vs stripped default) apply to the executor and all agent subprocesses in one hop. Moving the Popen
    call out just to dodge 2 lines would split supervision state (_process/_generation) across modules;
    revisit when supervisor grows its next feature, not for this.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/451
  ceiling: 231
- check: architecture.file_budget
  path: worker/worker_declarations.py
  reason: 'The deprecated-capabilities normalization moved here from config_validation (#444 rework) to
    free that file''s zero headroom for the proxy field; labels/models/capabilities are one family of
    scheduling declarations and share this validation module. The move is net-zero for the package; remove
    this exemption together with the capabilities compat key itself (tracked separately from the #284
    mechanism retirement) instead of re-splitting.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/452
  ceiling: 54
- check: architecture.file_budget
  path: server/app/routes/artifacts.py
  reason: Artifact routes authenticate the per-Worker token used by Agent execution transport, and the
    per-file upload path grew past the plain baseline with the scoped-token work (#35). Deletion was re-assessed
    for (#287) and rejected with evidence the CAS channel is still live it is the degradation path when
    object storage is optional (AGENT_LEGION_S3_* unset leaves JobArtifactObjectStore.enabled false, dispatch
    manifests then carry no artifact_uploads and Workers POST per-file), the Host completion accepts mixed
    sha256 refs, and min_protocol_version defaults to 1 so pre-D12 Workers are not fenced at registration.
    The durable resolution stays deleting this router once object storage is a required deployment and
    the retired Worker generation is fenced (re-anchored 160 then 287 then 300), not splitting deprecated
    code.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 48
- check: architecture.route_response_model
  path: server/app/routes/studio_chat.py:session_events
  reason: Studio chat session events are a Server-Sent Events stream (StreamingResponse, text/event-stream),
    same shape as the workspace job events route; there is no JSON response model to name.
  owner: workspace-executor
  remove_when: docs/architecture/studio-phase3-implementation-plan.md
- check: architecture.route_response_model
  path: server/app/routes/agent_workers.py:result
  reason: Agent Worker result acknowledgement returns an empty 204 response by protocol.
  owner: workspace-executor
  remove_when: docs/architecture/backend.md#agent-worker-protocol-responses
- check: architecture.route_response_model
  path: server/app/routes/agent_workers.py:release_slot
  reason: Agent Worker release-slot acknowledgement returns an empty 204 response by protocol.
  owner: workspace-executor
  remove_when: docs/architecture/backend.md#agent-worker-protocol-responses
- check: architecture.file_budget
  path: frontend/src/components/dag/dagNodeTypes.ts
  reason: The hover-highlight rework (#276) adds the two data-level boolean fields (active/dimmed) that
    make per-node memoization work; the interface is a flat field list where each line IS a feature, so
    the 2-line growth is the honest cost of the fix, not splittable structure.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 30
- check: architecture.file_budget
  path: frontend/src/components/dagHighlight.ts
  reason: The chained-comparison rework of hover highlighting (Codex P2 review on PR 285) grows this pure
    module by the prev-visual-state parameter, the prev/active node lookup maps, and the hover state machine
    -- the atomic prevActiveNode/hoveredNode update via a reducer is what prevents a wasted full recompute
    round between hovers.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 111
- check: architecture.file_budget
  path: server/app/routes/workflow_node_codes.py
  reason: The issue-211 Phase 2 URL-segment retirement keeps every historical workflow-key path registered
    as a deprecated alias next to the segment-free route (stacked decorators — the architecture doc generator
    only enumerates decorator-registered routes, so add_api_route would silently drop eight routes from
    backend.md). The deprecated aliases are the honest cost of the deprecation window; deleting them (Phase
    3/4 removal tracked in issue 211) drops the file back under the baseline ceiling and this exemption
    goes stale automatically. The Codex P2 fix on PR 299 adds _resolve_key (query-bound alias must equal
    the workspace id), which is part of the same deprecation-window cost.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 240
- check: architecture.file_budget
  path: server/app/routes/job_http.py
  reason: The issue-211 Phase 3 read-binding guard (reject_mismatched_workflow_key, shared by five explicit-key
    entry points per the Codex P1 on PR 307) lives here with the other route-layer HTTP helpers. The overage
    is the honest cost of the deprecation window; dropping the deprecated workflow_key request faces (Phase
    3/4, tracked in issue 211) removes the guard and this exemption goes stale automatically.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 47
- check: architecture.file_budget
  path: velites/src/agent.rs
  reason: 'Issue #297 added the first inline unit tests to the agent loop module (pure-function coverage:
    enabled_tool_names, outputs_remediation, resolve_required_outputs incl. sandbox-escape rejection).
    Rust inline #[cfg(test)] tests colocate with the implementation by convention (the budget policy itself
    exempts velites/tests/ for this reason), and the loop tests already live in velites/tests/agent_loop.rs;
    what remains inline needs private-fn access. Ceiling 282 to 331 covers the ~49 effective test lines;
    drop the exemption when the module shrinks or the pure helpers become pub and testable from tests/.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 329
- check: architecture.file_budget
  path: velites/src/cli.rs
  reason: 'Issue #297 added inline unit tests for the clap argument surface (flag parsing, defaults, trailing-command
    sandbox wrap, @file prompt expansion). Rust inline #[cfg(test)] convention (see policy comment on
    velites/tests/); the CLI structs are pub but the parse-behavior tests assert on private helper edges.
    Ceiling 91 to 261 covers the test module; drop when the module shrinks.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 260
- check: architecture.file_budget
  path: velites/src/session.rs
  reason: 'Issue #297 added inline unit tests for SessionLog (open creates the dir, append is NDJSON with
    order preserved, metadata round-trips, append-only never truncates). Rust inline #[cfg(test)] convention;
    SessionLog is pub but the tests assert the append-only mirror semantics next to the implementation.
    Ceiling 34 to 149 covers the test module; drop when the module shrinks.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 139
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodeAgentConfigBody.tsx
  reason: 'The PR 317 merge combines both sides of the agent-node inspector: develop''s type-gated WorkflowNodeAgentConfigBody
    (#284, with the approval-gate section) and the node-level skill binding editor (#76, WorkflowNodeSkillEditor
    between the definition card and the execution details). Each side fit its own ceiling; the honest
    union is ~7 effective lines past develop''s fresh 43-line registration. Same merge-growth class as
    the groups.py exemption; the body shrinks back when the approval section or the skill editor moves
    to its own module.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 55
- check: architecture.file_budget
  path: server/app/db/migrations/jobs_workflow_key_alignment.py
  reason: The issue-211 Phase 3 v68 data migration aligns every stored workflow_key with the v62 binding
    across the live tables plus the node_code entity-key prefix. Each composite-unique surface carries
    its collision rule — status-count merge (upsert plus delete), state-table twin drop, revision active-archive
    plus uniform version-shift, and the entity-key published-wins twin rules (twin pass precedes the demotion
    so frozen-pin hashes survive; both-published twins resolve to the window-era row), state-table multi-generation
    dedupe, and the re-prefix separator guard. The issue-211 M2 batch adds the post-v70 shape guard via
    the shared has_column probe (fresh databases already run the terminal shape; each pass skips its table
    instead of failing the replay). The overage is that single transaction's honest cost; M2 dropped the
    workflow_key columns, and M3 (retiring the deprecated contract fields) collapses these migration bodies
    and the exemption with them.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 208
- check: architecture.file_budget
  path: server/app/db/migrations/run_job_status_counts.py
  reason: 'Issue #358''s review round (P2 on #374) adds the dead-row recycle trigger (runs delete purges
    the counter rows): the counter table is deliberately FK-free because jobs.run_id is unconstrained
    text, so the recycle cannot ride a cascade like the v36 workspace twin. Both triggers and the backfill
    are one migration''s cohesive DDL; splitting the file would separate the sync function from its recycle
    pair.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 71
- check: architecture.file_budget
  path: server/app/services/run_partial_failure.py
  reason: 'codex round-1 #2 on PR #479: the two-branch chunked-creation compensation (empty-run removal
    vs partial-run failed-marking) moved into this module so the /runs items path and the /job-batches
    intake share ONE implementation and cannot drift. The error type, the operator message, and the compensation
    protocol are one concern; splitting them across modules would separate the failure protocol from its
    error contract.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 57
- check: architecture.file_budget
  path: server/app/db/migrations/__init__.py
  reason: 'The #358/#359 merge round registers two new migration functions (ops_runtime_profile_samples,
    run_job_status_counts); ruff''s isort normalizes each single-name import with a noqa marker to the
    parenthesized form, so the file grew by two lines against its baseline. The file is a pure re-export
    index whose growth model is one entry per migration module; revisit if the migrations package gains
    a programmatic registry.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 67
- check: architecture.file_budget
  path: server/app/db/migrations/runs_sql.py
  reason: The issue-211 M2 batch adds _INSERT_RUNS_SQL_V70 — the column-less twin of the job_batches cutover
    (without it a pre-v53 database upgraded to v70+ silently drops every historical batch row).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 134
- check: architecture.file_budget
  path: server/app/jobs/queries/batch_queue.py
  reason: The issue-211 M2 batch applies the same deprecated workflow_key identity backfill to the remaining
    runs-row write paths (intake claim, depleted requeue, intake update) so every runs-row consumer inherits
    the field until the 2026-10-31 removal window closes.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 78
- check: architecture.file_budget
  path: frontend/src/features/previewPanel/PreviewPanelHost.tsx
  reason: The Codex P1 review on PR 345 added the host-injected CSP (the outbound-network red line (sandbox
    allow-scripts does not block fetch/sendBeacon/img to external origins)). The policy constant, the
    srcDoc head-injection helper and its documentation grow the security surface in place -- splitting
    them out would separate the CSP from the iframe that enforces it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 189
- check: architecture.file_budget
  path: frontend/src/features/previewPanel/PreviewPanelSection.tsx
  reason: The Codex P2 review on PR 345 added bundle content into the PreviewPanelHost remount key plus
    the hashBundle helper -- a cross-navigation bridge-request isolation fix that must stay at the composition
    site to keep the in-flight-window reasoning local.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 77
- check: architecture.file_budget
  path: worker/runtime/preflight.py
  reason: '#381 moved velites/pi out of the worker image (runtime executors are externally-mounted binaries
    now), which turned "forgot to mount" from a rare deployment accident into a routine mistake with a
    silent failure mode — auto-detection yields an empty set and the worker registers with zero capacity.
    The expect-runtimes guard added here (env parsing, missing-detection against the runtime catalog,
    actionable error text covering the bundled dir and PATH) doubles the file past its first ceiling.
    The module stays a single cohesive concern (startup preconditions for the executor); splitting the
    guard into its own module was rejected because preflight_error would reduce to a shell over two helpers.
    Revisit the split if preflight grows a third guard.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 56
- check: architecture.file_budget
  path: worker/runtime/setup.py
  reason: '#381 wired AGENT_WORKER_EXPECT_RUNTIMES through prepare_runtime_models (env read + ValueError
    mapping into the shared startup-error string) alongside the existing resolve_config_runtimes handling,
    and the codex review on #384 extended it: model-discovery failures for an *expected* runtime now hard-fail
    startup (a wrong-arch binary passes the existence probe and only breaks at exec time), while unexpected
    runtimes keep the soft warning. This module is the composition point between config parsing, preflight,
    and model discovery; there is no meaningful split for the guard wiring.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 50
- check: architecture.file_budget
  path: server/app/jobs/queries/job_artifact_keys.py
  reason: 'Re-filed (systematic review round, PR #344): the GC storage-key query surface for job_artifacts
    and materials lives in one mixin (#340) — the systematic review found that material keys of a workspace
    literally named jobs / jobs-staging land inside the GC prefixes, so both the scan-time loader and
    the delete-time revalidation lookup had to union materials.storage_key; splitting the two tables across
    mixins would separate one reference set into two call sites that must stay in lockstep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/340
  ceiling: 24
- check: architecture.file_budget
  path: server/app/agent_broker/agent_result_commit.py
  reason: 'Issue #359 adds the execute-done counter at the terminal-commit point (two effective lines).
    The counter must fire exactly where the lease and request both reach their terminal state — moving
    it out would separate the gauge from the state transition it measures.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 65
- check: architecture.file_budget
  path: server/app/services/ops_metrics/service.py
  reason: 'Issue #359 extends the sampling pass to persist the runtime-profile gauge row (one call plus
    the pool/pool-backlog gauge readers kept next to their only consumer). The sampler stays one cohesive
    write surface; the durable split is a sampling-dispatch module if more gauge families join, at which
    point this exemption goes stale. #490 feeds the offline detector from the same last_seen read (one
    call, threshold restored to match the online count — review P0 on #494).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 189
- check: architecture.file_budget
  path: server/app/routes/__init__.py
  reason: Issue 329 mounts the job diagnosis observation router in the route assembly (one import + one
    secured() call, three effective lines past the baseline ceiling 137). The file is the single mount
    point for every feature router; the durable fix is splitting the assembly per domain, at which point
    this exemption goes stale.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 140
- check: architecture.file_budget
  path: server/app/events/buffer.py
  reason: Issue 353 swaps the per-event job_event_seq bump for segmented allocation (one UPDATE grants
    SEGMENT_SIZE revisions, spent from an in-process counter). The growth is a module-level constant with
    its trade-off docstring (segment size rationale, watermark-not-dense-index consumer contract, multi-replica
    reorder window) plus the segment window arithmetic in _next_revision_locked — all of it must stay
    next to the issuer it governs; splitting the allocator out would separate the deque-order invariant
    from the lock that enforces it. Revisit when the events package grows a second revision consumer.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/349
  ceiling: 112
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useAgentPublishRequest.ts
  reason: 'The #429 fix moves the resolved notice into a shared zustand store (per-instance useState was
    a dead feature — the dialog''s confirm/cancel wrote a notice the aside instance could never read)
    and observes the pending→null transition so an externally-superseded request (manual publish / newer
    agent request) lands a dismissable receipt instead of the dialog closing silently. The observation
    effect and the two refs belong with the confirm/cancel actions they disambiguate; the notice text
    builders already live in agentPublishNoticeStore. The fourth round (#429 四轮) adds publishInFlight
    (the whole-confirm window flag the dialog uses to show "publish in progress" on polled confirming
    rows) and teaches the observation effect to skip confirming rows (a live claim is not a terminal null
    jump — no bogus receipt). The closing round (#429 收尾 P3) makes a cancel 404 silent (the backend''s
    deliberate lost-to-the-claim answer is not an operation failure; the row state speaks for itself)
    while non-404 failures keep the toast. Revisit when issue 416''s deferred follow-ups (buffered rejection,
    session wiring) rework this hook.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 137
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/StudioChatAside.tsx
  reason: 'The #429 fix makes the resolved notice real: it renders from the shared store and is dismissable
    (a close button next to the message). The growth is the dismissable-notice markup; the aside stays
    the single place the receipt is displayed. Revisit when issue 416''s deferred follow-ups rework the
    aside layout.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 54
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/StudioChatDraftCards.tsx
  reason: '#692 draft-card redesign grew the module past its 67-line base budget: the MUI icon
    header, the per-entity publish gating (status-aware DraftPublishAction, R2 P2-1),
    honest-boundary comments, and the R4 workspaceId prop plumbing (required — job-diagnosis
    and customize-preview hosts render the cards under another workspace). The cards stay one
    module (three siblings of one family); the next growth should split the node-code card or
    move the publish actions to their own module. 110 effective lines (8 beyond the prior
    87+15 band, re-signed time-boxed per #641); harvest at the #456 sweep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 110
  expires: 2026-10-31
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/studioChatMessages.ts
  reason: '#692 draft-card lifecycle grew the extractor module past its 320 ratchet baseline in two
    steps: codex P1 round 2 added per-entity dedup (keepLatestPerEntity) and codex P1 round 3
    added the draft identity hash parsed from each save response (draftHashFromOutput) — publish
    must verify the card against the server-side draft because the entity is workspace-level
    state that another session can overwrite. 34 effective lines past baseline (19 beyond the
    +15 growth band), re-signed from the first exemption at 338 — time-boxed per #641. The
    module stays the single chat view-model (extraction + grouping); harvest at the #456 sweep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 354
  expires: 2026-10-31
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/EntityDraftPublishButton.tsx
  reason: '#692 grew past its 73-line ratchet baseline across review rounds: honest-boundary
    docstring + 404 friendly message (R2), the codex P1 round-3 server-side draft identity
    check (versions first-draft-row hash comparison), then the R4 hardening — workspaceId as a
    required prop (job-diagnosis/customize-preview hosts render under another workspace),
    publish-response hash comparison warning for the residual TOCTOU window, and a distinct
    message for versions-read failures. 128 effective lines (19 beyond the prior 109+15 band,
    re-signed time-boxed per #641); the button stays one focused component (identity check +
    publish + invalidation); harvest at the #456 sweep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 128
  expires: 2026-10-31
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/StudioChatPanel.module.css
  reason: '#429 added scopeNoteDismiss (resolved-notice close button) flexing scopeNote; the #692
    draft-card type redesign (MUI icon header, per-type title/icon tones, draftError, shared publish
    entry) re-signed the ceiling from 356 to cover the growth to 395 effective lines — beyond the
    +15 growth allowance band, so time-boxed per #641. After merging base''s #695 skeleton
    unification (shared panel rules moved out) the module sits at 374 effective lines; ceiling
    ratchets DOWN with it (monotonic discipline). The draft-card family moving to its own module
    remains the preferred next step; harvest at the #456 sweep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 374
  expires: 2026-10-31
- check: architecture.file_budget
  path: frontend/src/lib/queryKeysExtra.ts
  reason: 'Merge-growth class (PR 317 precedent): develop''s #419 connectionKeys and #427 nodeRuns entries
    plus this branch''s studioPublishRequest key (the agent publish request poll, #416) union to one effective
    line past the 65 ceiling each side satisfied alone. The module is a flat key registry; each line is
    one cache identity. Revisit at the next registry cleanup.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 66
- check: architecture.file_budget
  path: server/app/db/connection.py
  reason: '#438 makes __exit__ leak-proof (close in finally so a failed commit/rollback cannot strand
    the checkout) and stamps the checkout origin for the pool return-path telemetry. The 8 effective lines
    are the two documented guards; the attribution logic itself lives in pool_reset.py. Stays until the
    transaction helpers retire this facade.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 75
- check: architecture.file_budget
  path: server/app/db/pools.py
  reason: '#438 installs the pool reset callback (reset=reset_connection) so every returned connection
    is verified IDLE and dirty returns are attributed — one effective line plus wiring comment.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 50
- check: architecture.file_budget
  path: server/app/workflows/loader.py
  reason: 'Issue #458 round-trip fix: the asdict snapshot reloader must translate the serialized reduce.from_node
    spelling back to the yaml from the loader reads (mirroring the existing node_type translation), else
    every sharded/reduce revision snapshot fails reparse with "reduce.from is required". A ten-line guarded
    translation at the one dict->definition boundary; moving it out would fork the snapshot normalization
    the loader already owns. Revisit when snapshot loading gains its own module.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 299
- check: architecture.file_budget
  path: server/app/workflows/revision_format.py
  reason: '#458 grows definition_to_yaml past its 131 ceiling: the echo must carry the shard/reduce declarations
    (previously dropped — the studio''s initial draft YAML showed a ghost change against its own baseline
    and publishing it silently stripped fan-out) plus the apply_shard_echo helper extracted to keep the
    non-default-keys spelling next to its loader counterpart. The alternative, splitting the echo out
    of the revision format module, would fork the one serializer every revision/job-snapshot path shares.
    Remove when the revision echo moves to a schema-driven serializer or the file gets a structural split
    of its own.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 138
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/validation/workflowStudioChangeFormatters.ts
  reason: 'Issue #431 adds the remaining compare-field labels (node_type, after, shard, reduce) so the
    changes drawer never shows raw field names for the fields compare now diffs; the file is a flat label
    table where every line is a translation. Revisit when the label table moves to the generated change-contract
    types or gets i18n tooling.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 78
- check: architecture.file_budget
  path: velites/src/events.rs
  reason: 'Issue #469 adds ToolTiming to the event schema module: the phase struct (spawnMs / firstByteMs
    / restMs / reapMs with per-phase wire docs, including the firstByteMs observation semantics), the
    tool_execution_end wiring, and the wire-shape tests sit next to every other event type''s definition
    and contract test — the module IS the event schema. The growth is docstring + test lines over a ~50-line
    struct; dropping the docs to fit would trade the phase semantics (which phase covers what, when absent)
    for bookkeeping. Ceiling at actual + 10 per the dated-exemption convention (#431 / #458 precedents).
    Remove when the timing fields move into a schema-driven shape or the module gets a dedicated events-tools
    split. #577 moved StdoutJsonlSink out to velites/src/event_sink.rs; ceiling re-seated at the post-move
    actual + 10.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 484
- check: architecture.file_budget
  path: velites/src/tools/bash.rs
  reason: 'Issue #469 instruments the bash tool with the phase decomposition the stall forensics asked
    for: spawn timing, first-output-byte reading (read_with_first_byte replaces read_to_end with identical
    byte/error semantics; the first byte is the earlier of the two streams), the reap-phase branch, and
    the module docstring documenting the observation axes and the harness-read-vs-child-write caveat.
    The tool is one cohesive spawn/read/terminate flow; splitting instrumentation out would fork the child
    lifecycle across modules. Ceiling at actual + 10 per the dated-exemption convention (#431 / #458 precedents)
    — the module is in active observation tuning. Remove when #469''s follow-up (once the write() stall
    trigger is pinned) retires or reshapes the instrumentation.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 264
- check: architecture.file_budget
  path: velites/src/tools/mod.rs
  reason: 'Issue #469 review fix makes the ToolKind::execute dispatch boundary the single totalMs timing
    point (every tool kind gets it; the in-process tools carry zero instrumentation of their own — the
    uuid and validate exemptions retired when their timing code was deleted). mod.rs is the tools facade
    every tool already imports. Ceiling at actual + 10 per the dated-exemption convention (#431 / #458
    precedents). Remove when the file shrinks back under its old ceiling.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 155
- check: architecture.file_budget
  path: worker/executor.py
  reason: 'Issue #352 starts the per-Worker batch heartbeat coordinator in main(): three lines (import,
    the coordinator start wiring the upload queue to the same registry, the registry argument on the run_execution
    submit). The executor main is the single place where the machine-wide stop event and the UploadQueue
    live together; moving the wiring out would fork lifecycle ownership. Revisit when executor main gets
    its next split. Ceiling re-dated 226 to 224 after the rebase onto 0.7.0 (#481 ClaimPacing): #481''s
    import cleanup (agent_subprocess_env re-export retired, its test consumers import worker.execution.run
    directly) left 214 effective lines with this wiring on top, so the ceiling realigns to actual + 10
    per the dated-exemption convention (#431 / #458 precedents).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 220
- check: architecture.file_budget
  path: server/app/agent_runtime/velites.py
  reason: '#476 wires the velites static tool-catalog mirror (the `velites tools list --json` reflection
    pinned by the cross-binary contract test) into the adapter declaration — one dataclass field. The
    mirror itself lives in tool_catalog.py; this file only carries the reference. Remove together with
    the mirror if velites ever self-describes over the wire instead of via static declaration.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 16
- check: architecture.file_budget
  path: velites/src/main.rs
  reason: '#476 dispatches the `tools list` token before the agent-run CLI parse — the same pre-parse
    dispatch block as `models list` / `sandbox wrap` / `validate` (that CLI requires a positional, so
    subcommands must be peeled off first). The handler itself is one catalog call in tools/catalog.rs.
    Remove if the pre-parse dispatch moves into a shared argv router.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 81
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/AgentEditor.tsx
  reason: '#476 makes the tool picker catalog-driven: option-surface derivation (selectable/forced/invalid
    sets as memoized maps), the forced-tier locked-row hint, and the runtime-switch invalid-tool banner
    join the existing form lifecycle. The catalog plumbing is already split out (useAgentRuntimes hook);
    what stays here is the interaction itself — splitting render from state would separate the two halves
    of every conditional being tested. Remove when the editor gains a shared tool-picker component with
    WorkflowNodeToolsEditor.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 357
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/workflowStudioYamlDraft.ts
  reason: '#443/#476 add patchWorkflowNodeTools — one patchNode call site with the empty-means-undeclared
    delete-key semantics, same shape as the sibling patch functions in this table. Remove together with
    the node-level tools declaration if it ever retires.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 121
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/workflowStudioYamlDraft.parse.ts
  reason: '#443: WorkflowYamlNode gains the optional tools field (one line plus its contract comment).
    The node shape lives in one type by design. Remove with the field itself.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 83
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowAgentExecutionDetails.tsx
  reason: '#443/#476 mount WorkflowNodeToolsEditor below the runtime settings — one import plus one JSX
    block (9 lines) in the agent execution body. The editor itself is a sibling component; this file is
    only the composition point. Remove if the composition is restructured.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 65
- check: architecture.file_budget
  path: velites/src/tools/specs.rs
  reason: '#518 adds the json tool''s provider spec (description + JSON Schema parameters) — the file
    is the per-tool spec table by design, one arm per ToolKind; the spec IS the content, there is nothing
    to split without separating a tool''s spec from its siblings'' in a way no consumer wants. Remove
    only with the json tool itself.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 112
- check: architecture.file_budget
  path: velites/src/config.rs
  reason: '#602 adds the deprecation notice pair (file-present + env-override arms, ~20 effective lines:
    the migration-target message, the models.json pointer, and the removal-window statement) to a file
    that is scheduled for wholesale deletion in the next release cycle — the issue''s removal phase drops
    config.rs and the lib.rs gateway fallback branch together. Ceiling at actual + 10 per the dated-exemption
    convention; harvest together with the deletion.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/602
  ceiling: 200
- check: architecture.file_budget
  path: velites/src/lib.rs
  reason: '#476/#518: lib.rs carries the forced-tool activation at the one point where all three inputs
    exist (parsed --tools, canonicalized skill dirs, --require-output) and the tools-listing error message
    that must name the full tool set. The activation is a harness run-assembly concern, not a tool-module
    one. Remove if the run assembler is extracted from the provider wiring.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 211
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodePromptEditor.tsx
  reason: '#513 adds the prompt-mode selector (append/overwrite) to the custom prompt editor: one TextField
    select with two labeled options plus the state-switching hint line — the interaction the whole feature
    is about. The data layer lives in useNodePromptPreview; splitting the selector out would separate
    the mode from the textarea it governs. Remove if the prompt panel is restructured.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 82
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/useNodePromptPreview.ts
  reason: '#513: the hook derives the prompt mode from the draft YAML (junk-tolerant normalization mirroring
    the prompt field) and exposes onPatchMode — data-layer counterparts of the editor''s mode selector.
    Same P1 normalization family as customPrompt above it. Remove with the mode selector.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 98
- check: architecture.file_budget
  path: server/app/services/node_prompt_preview.py
  reason: '#513: the preview payload gains prompt_mode (normalized to append) and save_node_prompt accepts
    it — the API surface of the mode feature. The service is the single preview/save implementation shared
    by the Studio route and the MCP tools; the growth is the feature, not drift. Remove if prompt previewing
    is extracted from the draft-editing service.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 161
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodeToolsEditor.tsx
  reason: 'codex P2 on #527: invalid declared tools render as removable chips (MUI multi-select cannot
    deselect a disabled option, so the removal needs its own surface) — one Chip row inside the existing
    alert. The editor is already the node-level counterpart of AgentEditor''s picker. Remove together
    with the AgentEditor tool-picker exemption.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 90
- check: architecture.file_budget
  path: server/app/routes/instance_settings_contracts.py
  reason: 'Issue #509/#554/#561 folds three capacity knob blocks (agent_enqueue, result_unpack, agent_claim)
    into the instance settings document: three small nested contract models plus three fields on InstanceSettingsDocument;
    issue #591 adds the result_commit_batching kill-switch bool to the agent_workers block (one line,
    the same nested-block shape). The nested models must sit beside their siblings (InstanceWorkflowsSettings
    / InstanceAgentWorkersSettings) — the document IS the admin API surface; splitting two-field blocks
    into a sister module would fork the contract from the file that owns its shape. Ceiling at actual
    + 10 per the dated-exemption convention. Remove when the document gets a structural split (per-block
    modules) or the next retirement frees headroom.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 55
- check: architecture.file_budget
  path: server/app/mcp_server/skill_tools.py
  reason: 'Issue #633 adds the create_skill tool to the skill tool group: one more tool function (docstring
    is the budget-heavy part — every tool''s contract text lives in its docstring) beside get/validate/save_version.
    The module IS the single skill-tool registration surface; splitting one tool into a sister module
    would fork the group''s shared client_factory wiring and the URL-encoding helper. Ceiling at actual
    + 10 per the dated-exemption convention. Remove when the skill tool group gets a registration split
    per tool or a budget re-balance frees headroom.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 48
- check: architecture.file_budget
  path: server/app/mcp_server/server.py
  reason: 'Issue #633 merges three tool groups into the registration sequence (draft CAS pair, shared-material
    pair, agent-definition read trio) and the inline save_agent_definition_draft gains the requires_labels/
    config_schema parameters. Each group''s register_* call plus its guard comment is one effective line
    over the frozen 103 baseline; the module is the single composition root and every tool group that
    CAN live in a sibling module already does. Remove when the last inline tools (get_authoring_guide
    / get_studio_context / save_node_code_draft / save_agent_definition_draft) move to sibling modules.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 104
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/AgentPublishRequestDialog.tsx
  reason: 'The #429 fourth round (#429 四轮 P2-1/P3-2) makes the confirm''s draft flush a completion-order
    await (flushDraftSave returns the PUT promise; the server-draft re-read and the confirm endpoint fire
    only after it resolves — the old fire-and-forget could publish a stale draft whose hash happened to
    match) and teaches the dialog to hold itself open on polled confirming rows (publishInFlight keeps
    the buttons disabled mid-publish instead of the dialog vanishing). The growth is the try/catch around
    the flush/read-draft chain plus the error toast the review asked for. Revisit when the deferred follow-ups
    (a shared useConfirmPublish hook) rework the dialog''s action wiring.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 58
- check: architecture.file_budget
  path: server/app/agent_runtime/tool_catalog.py
  reason: '#518 adds the json tool''s entry to the velites static mirror — the file is the declared mirror
    of `velites tools list --json`, pinned equal by the cross-binary contract test; its size is the catalog''s
    size plus the pi triple. The entry must live here in full (description/parameters verbatim) or the
    parity test fails by design. Remove if the mirror is replaced by runtime probing.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 192
- check: architecture.file_budget
  path: server/app/routes/studio_agent_catalog_read_tools.py
  reason: >-
    Issue #633 adds the workspace-scoped agent-definition create entry to
    the agent-definition tool group: one POST handler (capability-derived
    agent_id, studio-agent attribution) beside the read trio and the save
    draft. The module IS the single agent-definition tool endpoint surface;
    splitting one endpoint into a sister module would fork the group's
    shared router guards and the version-response helper. Ceiling at
    actual + 10 per the dated-exemption convention. Remove when the
    agent-definition tool endpoints get a route split per verb or a budget
    re-balance frees headroom.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 116
- check: architecture.file_budget
  path: server/app/mcp_server/agent_tools.py
  reason: >-
    Issue #633 adds the create_agent_definition tool to the agent tool
    group: one more tool function (docstring is the budget-heavy part —
    every tool's contract text lives in its docstring, and the create spells
    out the capability-derivation + 409 semantics) beside the read trio.
    The module IS the single agent-tool registration surface; splitting one
    tool into a sister module would fork the group's shared client_factory
    wiring. Ceiling at actual + 10 per the dated-exemption convention.
    Remove when the agent tool group gets a registration split per tool or
    a budget re-balance frees headroom.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 38
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/WorkflowStudioDraftSaveControl.tsx
  reason: >-
    Issue #633 kimi review adds the conflict exit UI (adopt/keep-mine action buttons) to the draft-save
    conflict loop. The growth is the conflict-UX feature itself (the P1
    findings demanded explicit exits instead of a one-key silent override);
    splitting the conflict lifecycle out of the save controller would fork
    the CAS base bookkeeping it guards. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the draft-save wiring gets its
    planned rework or a budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 83
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/serverDraftReapply.ts
  reason: >-
    Issue #633 kimi review adds the own-save echo detection (canvasYaml content compare) to the draft-save
    conflict loop. The growth is the conflict-UX feature itself (the P1
    findings demanded explicit exits instead of a one-key silent override);
    splitting the conflict lifecycle out of the save controller would fork
    the CAS base bookkeeping it guards. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the draft-save wiring gets its
    planned rework or a budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 98
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useServerDraftApply.ts
  reason: >-
    Issue #633 kimi review adds the canvasYaml wiring for the echo detection to the draft-save
    conflict loop. The growth is the conflict-UX feature itself (the P1
    findings demanded explicit exits instead of a one-key silent override);
    splitting the conflict lifecycle out of the save controller would fork
    the CAS base bookkeeping it guards. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the draft-save wiring gets its
    planned rework or a budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 47
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useWorkflowStudio.ts
  reason: >-
    Issue #633 kimi review adds the conflict exit fields on the studio API to the draft-save
    conflict loop. The growth is the conflict-UX feature itself (the P1
    findings demanded explicit exits instead of a one-key silent override);
    splitting the conflict lifecycle out of the save controller would fork
    the CAS base bookkeeping it guards. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the draft-save wiring gets its
    planned rework or a budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 91
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useWorkflowStudioDraftStore.ts
  reason: >-
    Issue #633 kimi review adds the conflict exit wiring + canvasYaml feed to the draft-save
    conflict loop. The growth is the conflict-UX feature itself (the P1
    findings demanded explicit exits instead of a one-key silent override);
    splitting the conflict lifecycle out of the save controller would fork
    the CAS base bookkeeping it guards. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the draft-save wiring gets its
    planned rework or a budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 65
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/draftSaveController.ts
  reason: '#429 rounds four and closing: flushNow/save return the PUT promise the agent-publish
    confirm awaits (#427). The state machine keeps its single-file shape by design:
    splitting it would fork the requestId/timer/CAS-baseline invariants it guards.
    #633 adds the conflict lifecycle (enter/resolve/adopt + autosave suspension);
    pure state transitions moved to draftSaveConflict.ts, and codex R3 P1 adds the
    PUT serialization (queued saves re-issue with the advanced CAS baseline; the
    queue/drain/schedule orchestration lives in draftSaveQueue.ts). The 171→182
    ceiling bump rides the develop release train (AGENT_LEGION_BUDGET_MONOTONICITY_
    RELEASE_TRAIN; #633 is the planned draft-save wiring rework this exemption
    waited for). Remove when the queue module absorbs the remaining save wiring
    or a budget re-balance re-registers it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/429
  ceiling: 182
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useDraftServerSync.ts
  reason: >-
    Issue #633 kimi review P1-2 adds the conflict exits (adoptServerDraft /
    resolveConflict) to the server-draft wiring hook — they belong beside the
    conflict surfacing they complete (the same hook drives enterConflict on
    the server advance). The file was created earlier in this same PR (#633
    codex review P1-2 reapply wiring) and the initial ratchet landed mid-PR
    at a smaller shape. Ceiling at actual + 10.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 69
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useWorkflowDraftPersistence.ts
  reason: '#429 closing P2-1: flushNow now returns the DraftSaveFlushResult (the live terminal state,
    not the React useState snapshot the dialog''s closure captured at click time). #633 adds the
    conflict-exit passthrough (adoptServerDraft/resolveConflict — kimi review P1-2; the exits live
    in useDraftServerSync and flow through here) and the reapply wiring consumes the same hook.
    Revisit when the draft-save wiring gets its planned rework.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/429
  ceiling: 95
- check: architecture.file_budget
  path: worker/execution/execution_lane.py
  reason: '#647 PR #648 复审加固（codex round 2 + subagent 双审）：退休交接
    re-check（_retire_if_idle，防 idle 超时窗口提交的任务永久滞留）、spawn
    判定与插入合并为临界区原子裁决（_spawn/_spawn_locked，防并发突破
    max_workers）、take→finish 全区 try/finally + future claim。防御性正确性
    代码使有效行 106→127（超 growth_allowance 带）。时间盒：phase-3
    remainder（run_execution 队列驱动 continuation 重写，见 exit_watch.py
    docstring）落地时 lane 结构重排并重登预算。'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/647
  ceiling: 127
  expires: 2026-12-31
- check: architecture.file_budget
  path: worker/execution/exit_watch.py
  reason: '#647 PR #648 复审加固：watcher 忙旋修复（_tick_timeout/_tick 排除
    done-set waiter）、同 pid 双注册唤醒被顶 waiter、reactor 两条死亡路径的
    pidfd 排水 + 自身资源关闭（kqueue/selector/wakeup pipe，防持续故障下
    fd 泄漏）。pidfd 管线已拆 exit_watch_pidfd.py，剩余防御性正确性代码使
    有效行 328→353（超 growth_allowance 带）。时间盒：同 execution_lane.py
    ——phase-3 continuation 重写时重排。'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/647
  ceiling: 353
  expires: 2026-12-31
