exemptions:
- check: architecture.file_budget
  path: server/app/skills/commit_cache.py
  reason: >-
    PR #571 codex P1s restored the per-validation private copy on top of the
    shared commit cache: materialized_private_copy plus the lock-held
    _cached_commit_tree split (cache probe/materialize/evict in one FileLock
    hold with the copytree) grew the module past its first-registration
    ceiling. The copy semantics belong to the cache that owns the lock and
    the eviction — splitting them out would fork the single lock domain.
    Ceiling at actual + 5 per the dated-exemption convention. Remove when
    the module gets a structural split (cache vs private-copy facade) or the
    next budget re-balance pass re-registers it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/569
  ceiling: 166
- check: architecture.file_budget
  path: server/app/agent_broker/result_validate_pool.py
  reason: >-
    PR #571 codex P1s: the pool task regained the per-validation private
    copy (materialized_private_copy + try/finally cleanup_execution), which
    the first-registration ceiling predated. The task must stay beside the
    pool it runs on (spawn importability and the BrokenProcessPool contract
    are documented together). Ceiling at actual + 5 per the dated-exemption
    convention. Remove when the pool/task split is re-budgeted or the env
    override channel retires (same lifecycle as the #554 unpack pool).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/569
  ceiling: 135
- check: architecture.file_budget
  path: server/app/workflows/output_contract_engine.py
  reason: >-
    Issue #538 adds the in-process contract-block probe (_has_contract_block)
    in front of the velites spawn: a skill that declares no contract block
    would only buy the engine's existence-mode no-verdict, so the subprocess
    is skipped and the validate stage returns to sub-second latency. The
    probe mirrors velites's fence scan (exact trimmed "```yaml contract"
    line, missing document degrades, everything fail-closed reports True) and
    must stay beside the spawn it gates — splitting it out would fork the
    degradation semantics from the engine seam that owns them. Ceiling at
    actual + 10 per the dated-exemption convention (#431 / #458 precedents).
    Remove when the velites engine itself grows an in-process Host library
    form or the two-layer validation restructure re-budgets the module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/538
  ceiling: 78
- check: architecture.file_budget
  path: frontend/src/pages/globalSettings/instanceSettingsCapacityFields.ts
  reason: >-
    Issue #569 adds the result-validate pool knob beside its #554
    result_unpack sibling: one field entry (path, label, integer, allowZero,
    max) in the same table shape. The capacity-knob group IS the form
    surface for the executor-runtime pool sizes; splitting a one-entry
    overflow into a sister module would fork the group. Remove when the
    instance-settings form gets its planned structural split or a knob
    retirement frees headroom.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/569
  ceiling: 47
- check: architecture.file_budget
  path: frontend/src/pages/globalSettings/instanceSettingsPayload.ts
  reason: >-
    Issue #569 adds the result-validate pool knob to the instance-settings
    form: one toFormValues line plus one buildPayload block, the same shape
    as its #554 result_unpack sibling (the prettier 80-column wrap forces
    the block over three lines). The form value/payload mapping IS this
    table; splitting two entries out would fork the mapping from the form
    it serves. Remove when the instance-settings form gets its planned
    structural split or a knob retirement frees headroom.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/569
  ceiling: 132
- check: architecture.file_budget
  path: frontend/src/pages/globalSettings/instanceSettingsFields.ts
  reason: >-
    Issue #521 adds the result-commit gate knob to the Worker-limits field
    group: one field entry (path, label, integer, allowZero) in the same
    table shape as its agent_workers siblings. The list IS the form surface.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 122
- check: architecture.file_budget
  path: server/app/agent_control/completion.py
  reason: >-
    Issue #521 adds the stage boundary marks (unpack/artifacts_verify/
    validate/artifacts_upload) inline between the physical steps they
    separate; each mark is one line at the exact boundary it names. The
    marks retire with the stage split's phase 2 rework (per-stage slimming
    restructures finish() and re-budgets the module).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 184
- check: architecture.file_budget
  path: server/app/executors/_lease_write_paths.py
  reason: >-
    Issue #521 closes the lease_write and events stage marks around the
    finish() body's two already-physical segments (terminal write tx; the
    events.jsonl post-processing). The marks are boundary annotations, not
    new logic; they retire with the events single-pass merge (0.7.3).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 166
- check: architecture.file_budget
  path: server/app/executors/leases.py
  reason: >-
    Issue #521 threads the optional stage_timer keyword through finish()
    into _lease_write_paths; the parameter must ride the same retry wrapper
    as the result it instruments (a retry re-runs the stages it re-measures).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 133
- check: architecture.file_budget
  path: server/app/services/path_hygiene.py
  reason: >-
    Issue #521 adds the per-path warn dedupe (set + lock + keyed signature)
    and the one-time rewrite orchestration (rebase rule, chunk loop, two
    daemon-thread kickers). The rewrite rides the facade queries added in
    jobs/queries/path_hygiene; the service keeps the warnings/report/rewrite
    story in one module. Shrinks back once every deployment's stored
    absolute-path rows have been rewritten and the rewrite pass can drop.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 150
- check: architecture.file_budget
  path: server/app/services/runtime_profile/counters.py
  reason: >-
    Issue #521 splits the stage-gauge families out (stage_gauges.py owns
    the tuples and folds); the registry keeps the counter table, the
    reset/snapshot loops, and the family-wide note_* methods. The #448
    exemption retired with that split; this one tracks the new floor.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 170
- check: architecture.file_budget
  path: server/app/services/runtime_profile/sampling.py
  reason: >-
    Issue #521 splits the wide-window rollup out (rollup.py) and widens
    the persisted values with the result-stage comprehension; the value
    marshalling stays with its only caller. The #359 exemption retired
    with the split; this one tracks the new floor.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 98
- check: architecture.file_budget
  path: server/app/jobs/queries/runtime_profile.py
  reason: >-
    Issue #521 widens the upsert column list with the result-stage
    comprehension (fourteen columns), the same single-source shape the v78
    claim-stage split added. The list IS the column contract; splitting it
    would fork the sync the migration test pins.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 103
- check: architecture.file_budget
  path: server/app/routes/runtime_profile_contracts.py
  reason: >-
    Issue #521 mirrors the fourteen result-stage gauge columns into
    ProfileBucket, the same field-list growth the v78 claim-stage split
    made. The model IS the response contract.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/521
  ceiling: 61
- check: architecture.file_budget
  path: worker/cli_args.py
  reason: >-
    The workerctl --proxy flag (#444 rework) gives headless workers (Docker
    form, console behind an ssh tunnel) the same config surface the UI
    already has: the console path needs a tunnel and hand-editing the
    mounted yaml triggers the supervisor divergence warning without taking
    effect. Five lines of honest cost (one argparse declaration with its
    help text, one payload mapping entry); the file is a flat argv table
    where each line IS a flag — nothing to split without forking the
    parser. Remove together with the supervisor spawn split (#451).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/451
  ceiling: 105
- check: architecture.file_budget
  path: worker/supervisor.py
  reason: >-
    The proxy config injection (#444) adds the env override dict at the
    single executor spawn site — the one place where os.environ is
    snapshotted into the child — so proxy semantics (configured proxy vs
    stripped default) apply to the executor and all agent subprocesses in
    one hop. Moving the Popen call out just to dodge 2 lines would split
    supervision state (_process/_generation) across modules; revisit when
    supervisor grows its next feature, not for this.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/451
  ceiling: 237
- check: architecture.file_budget
  path: worker/worker_declarations.py
  reason: >-
    The deprecated-capabilities normalization moved here from
    config_validation (#444 rework) to free that file's zero headroom for
    the proxy field; labels/models/capabilities are one family of
    scheduling declarations and share this validation module. The move is
    net-zero for the package; remove this exemption together with the
    capabilities compat key itself (tracked separately from the #284
    mechanism retirement) instead of re-splitting.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/452
  ceiling: 68
- check: architecture.file_budget
  path: server/app/agent_broker/claim.py
  reason: >-
    The claim transaction module stays one file for two stacked
    instrumentation generations: the #448 claim-stage timer (one
    ClaimStageTimer whose worker_setup/scan/evaluate/writes closes ride the
    existing statements, plus the report_claim_stages helper shared by all
    three return paths) and the #490 observation call (one
    note_claim_outcome per exit, payloads assembled in worker_events.py).
    The alternative — moving the orchestration out to a new module — would
    split the claim transaction's single flow across files for ~20 lines of
    instrumentation. The original anchor (#448) closed with phase 2
    restructured elsewhere; re-anchored to #490 by the structured-events
    PR that added the observation calls riding the same exits. #498 then
    REMOVED the in-transaction observation calls (the verdict rides out as
    one ClaimOutcome return; the broker emits only after the commit), so
    the ceiling stays at its committed floor. #546 moved the Worker-level
    setup out to claim_setup.py (shared with the batch claim) and the
    exemption ceiling ratcheted down with the shrink.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/498
  ceiling: 106
- check: architecture.file_budget
  path: server/app/agent_broker/worker_events.py
  reason: >-
    The #494 review round on the structured-events PR (#490) grows this
    module past its first-registered ceiling with three never-raise /
    classification fixes that belong next to the code they guard: the
    claimed_at read inside note_execution_finished gets the try/except the
    module's never-raise contract always demanded (a pool-exhausted read
    must not turn a committed 204 into a 500), _event_level gains the
    outcome=rejected INFO branch (the last Host-side clue of a refused
    execution), and the pool-state synthesis in note_claim_outcome is
    confined to the scan_skipped pass (agent full + drained code queue is
    the idle rhythm, not a capacity_full rejection). Splitting the emitters
    would fork the event-name/level/reason registries the module exists to
    keep single-source. Remove when the events module gains its own
    structural split (aggregation work) or the #490 family closes and the
    module is re-budgeted. Ceiling re-dated 292 to 294 for the #497 codex
    P2: the offline memo overwrites on every online bucket (setdefault
    froze the first observation, aging worker.offline's last_seen_at by
    hours) — one overwritten assignment plus its why-comment. #499 and PR
    #497 review round #5125358408 then added the shared HEARTBEAT_*
    reason literals, the saturation→DEBUG classification (P1-A) and the
    isEnabledFor serialization guard, all paid for inside the SAME 294
    ceiling by compacting the module docstring, the set literals and the
    alias indirection — no re-date.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/499
  ceiling: 294
- check: architecture.file_budget
  path: server/app/agent_broker/claim_windows.py
  reason: >-
    Issue #461 review on PR #461: scan_kind passes the #448 stage timer
    through to evaluate_candidate so the promote writes close inside the
    per-candidate call (see claim_evaluate.py's entry) — one keyword
    argument threaded through the existing signature plus its docstring
    line. Splitting the per-kind ladder out for that would fork the
    SCAN_ROUNDS budget logic the module exists to own. Remove when #448's
    phase-2 rework restructures the claim path and re-budgets the module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/448
  ceiling: 77
- check: architecture.file_budget
  path: server/app/db/migrations/job_status_counts_statement_triggers.py
  reason: >-
    Issue #437's #441 review round grows the v77 trigger module past its
    first-registered ceiling: the negative-net-delta arm is sign-split
    (positive delta upserts, negative delta is a bare UPDATE) so a missing
    (key, status) counter row gets no phantom cnt=-1 row — the v36/v73 row
    triggers' bare-UPDATE no-op, experimentally pinned — and the module
    docstring gains the honest trigger-count paragraph (executemany is
    still N independent INSERTs server-side; the win is the fixed lock
    order). The DDL template is the single source both trigger families
    splice from; splitting it out would fork the lock-order fix's one
    source of truth. Remove when the counter triggers retire or the
    migration module gets a structural split of its own.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/448
  ceiling: 143
- check: architecture.file_budget
  path: server/app/jobs/queries/workspace.py
  reason: >-
    The publish pipeline's override prune had to land inside the revision
    transaction (codex 终轮 P1-3 on #428), and the section-scoped
    node_config_json write (FOR UPDATE re-read, replace only the publishing
    workflow's section) lives on this mixin because every other workspace
    column write already does — a service-layer helper would need the raw
    connection this mixin owns. The durable resolution is splitting the
    workspace-node-override writes (node config + node limits) into their
    own queries mixin when the next workspace write arrives; the exemption
    goes stale with that split or whenever the file shrinks under 301. The
    original anchor (#418) closed with the panel work stranded on its
    stacked base branch instead of develop; re-anchored to #456 by the
    backport that finally lands the code-node panel and the override prune.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 332
- check: architecture.file_budget
  path: server/app/routes/artifacts.py
  reason: Artifact routes authenticate the per-Worker token used by Agent execution transport, and the
    per-file upload path grew past the plain baseline with the scoped-token work (#35). Deletion was re-assessed
    for (#287) and rejected with evidence the CAS channel is still live it is the degradation path when
    object storage is optional (AGENT_LEGION_S3_* unset leaves JobArtifactObjectStore.enabled false, dispatch
    manifests then carry no artifact_uploads and Workers POST per-file), the Host completion accepts mixed
    sha256 refs, and min_protocol_version defaults to 1 so pre-D12 Workers are not fenced at registration.
    The durable resolution stays deleting this router once object storage is a required deployment and
    the retired Worker generation is fenced (re-anchored 160 then 287 then 300), not splitting deprecated
    code.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 61
- check: architecture.route_response_model
  path: server/app/routes/studio_chat.py:session_events
  reason: Studio chat session events are a Server-Sent Events stream (StreamingResponse, text/event-stream),
    same shape as the workspace job events route; there is no JSON response model to name.
  owner: workspace-executor
  remove_when: docs/architecture/studio-phase3-implementation-plan.md
- check: architecture.route_response_model
  path: server/app/routes/agent_workers.py:result
  reason: Agent Worker result acknowledgement returns an empty 204 response by protocol.
  owner: workspace-executor
  remove_when: docs/architecture/backend.md#agent-worker-protocol-responses
- check: architecture.route_response_model
  path: server/app/routes/agent_workers.py:release_slot
  reason: Agent Worker release-slot acknowledgement returns an empty 204 response by protocol.
  owner: workspace-executor
  remove_when: docs/architecture/backend.md#agent-worker-protocol-responses
- check: architecture.route_response_model
  path: server/app/routes/agent_worker_claims.py:claim
  reason: >-
    #546 batch claim: one endpoint answers two protocol generations — the
    single AgentClaimResponse (limit=1, byte-identical for pre-#546 Workers)
    and the BatchAgentClaimResponse wrapper (limit>1). The named union
    response_model serializes as anyOf, which the inline-schema gate rejects
    by construction; both members are named contract models, so no untyped
    payload crosses. Removed together with the single-claim path (#547).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/547
- check: architecture.file_budget
  path: server/app/services/agent_service.py
  reason: Schema v27 made this service the only Agent catalog source of truth; the short-TTL published-definition
    cache (hot claim-path reads) lives next to the lifecycle methods so publish/archive can invalidate
    it in one place. Split the cache into its own module when it gains more policy — the dsn-identity
    cache-key helper (#187) is one of the call sites the ConnectSource retirement (#280) will rework,
    which is the natural split window. The original anchor (#196) closed with the studio_chat split without
    touching this service.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/280
  ceiling: 135
- check: architecture.file_budget
  path: server/app/services/agent_definition_create.py
  reason: >-
    The #460 release-review P1 fix extended the default-id occupancy check from
    the latest row per entity to three faces (the published rows a renamed draft
    hides, plus an entity keyed by the capability itself whose draft renamed it)
    — the scan is one cohesive policy that belongs with the create entry it
    guards, and each face cites the data-loss or unpublisable-entity branch it
    closes. Drop together with the #407 endgame (#440), which retires the
    entity mechanism this policy polices.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/440
  ceiling: 64
- check: architecture.file_budget
  path: server/app/storage/s3_client.py
  reason: The job-detail artifact preview (issue 11) added open_range (ranged GET) to the storage protocol
    and the S3 client implementation so object-store-only media can serve browser Range requests (seekable
    playback without whole-object downloads). The protocol and its S3 implementation must stay in one
    file; revisit when the storage layer grows a second backend implementation.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/11
  ceiling: 155
- check: architecture.file_budget
  path: frontend/src/components/dag/dagNodeTypes.ts
  reason: The hover-highlight rework (#276) adds the two data-level boolean fields (active/dimmed) that
    make per-node memoization work; the interface is a flat field list where each line IS a feature, so
    the 2-line growth is the honest cost of the fix, not splittable structure.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 33
- check: architecture.file_budget
  path: frontend/src/components/dagHighlight.ts
  reason: The chained-comparison rework of hover highlighting (Codex P2 review on PR 285) grows this pure
    module by the prev-visual-state parameter, the prev/active node lookup maps, and the hover state machine
    -- the atomic prevActiveNode/hoveredNode update via a reducer is what prevents a wasted full recompute
    round between hovers.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 121
- check: architecture.file_budget
  path: server/app/jobs/queries/groups.py
  reason: The JobQueries facade gained two new domain mixins from both sides of the PR 285 merge -- GlobalSettingsKVQueriesMixin
    (KV centralization, issue 281 on this branch) and ApprovalDecisionQueriesMixin (human-approval node,
    PR 266 on develop). Each is a legitimate one-line composition entry; the combined file is one effective
    line past the stale ceiling.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 78
- check: architecture.file_budget
  path: server/app/routes/workflow_node_codes.py
  reason: The issue-211 Phase 2 URL-segment retirement keeps every historical workflow-key path registered
    as a deprecated alias next to the segment-free route (stacked decorators — the architecture doc generator
    only enumerates decorator-registered routes, so add_api_route would silently drop eight routes from
    backend.md). The deprecated aliases are the honest cost of the deprecation window; deleting them (Phase
    3/4 removal tracked in issue 211) drops the file back under the baseline ceiling and this exemption
    goes stale automatically. The Codex P2 fix on PR 299 adds _resolve_key (query-bound alias must equal
    the workspace id), which is part of the same deprecation-window cost.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 259
- check: architecture.file_budget
  path: server/app/routes/studio_agent_tools.py
  reason: Same issue-211 Phase 2 dual-path deprecation as workflow_node_codes.py — the studio-agent node-code
    tool routes keep their historical workflow-key alias registered as deprecated alongside the segment-free
    path. Removing the aliases with the Phase 3/4 URL cleanup in issue 211 retires this exemption. The
    Codex P2 fix on PR 299 adds _resolve_key (query-bound alias must equal the workspace id), which is
    part of the same deprecation-window cost.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 192
- check: architecture.file_budget
  path: server/app/routes/job_http.py
  reason: The issue-211 Phase 3 read-binding guard (reject_mismatched_workflow_key, shared by five explicit-key
    entry points per the Codex P1 on PR 307) lives here with the other route-layer HTTP helpers. The overage
    is the honest cost of the deprecation window; dropping the deprecated workflow_key request faces (Phase
    3/4, tracked in issue 211) removes the guard and this exemption goes stale automatically.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 56
- check: architecture.file_budget
  path: velites/src/agent.rs
  reason: 'Issue #297 added the first inline unit tests to the agent loop module (pure-function coverage:
    enabled_tool_names, outputs_remediation, resolve_required_outputs incl. sandbox-escape rejection).
    Rust inline #[cfg(test)] tests colocate with the implementation by convention (the budget policy itself
    exempts velites/tests/ for this reason), and the loop tests already live in velites/tests/agent_loop.rs;
    what remains inline needs private-fn access. Ceiling 282 to 331 covers the ~49 effective test lines;
    drop the exemption when the module shrinks or the pure helpers become pub and testable from tests/.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 331
- check: architecture.file_budget
  path: velites/src/cli.rs
  reason: 'Issue #297 added inline unit tests for the clap argument surface (flag parsing, defaults, trailing-command
    sandbox wrap, @file prompt expansion). Rust inline #[cfg(test)] convention (see policy comment on
    velites/tests/); the CLI structs are pub but the parse-behavior tests assert on private helper edges.
    Ceiling 91 to 261 covers the test module; drop when the module shrinks.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 261
- check: architecture.file_budget
  path: velites/src/session.rs
  reason: 'Issue #297 added inline unit tests for SessionLog (open creates the dir, append is NDJSON with
    order preserved, metadata round-trips, append-only never truncates). Rust inline #[cfg(test)] convention;
    SessionLog is pub but the tests assert the append-only mirror semantics next to the implementation.
    Ceiling 34 to 149 covers the test module; drop when the module shrinks.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/311
  ceiling: 149
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodeAgentConfigBody.tsx
  reason: 'The PR 317 merge combines both sides of the agent-node inspector: develop''s type-gated WorkflowNodeAgentConfigBody
    (#284, with the approval-gate section) and the node-level skill binding editor (#76, WorkflowNodeSkillEditor
    between the definition card and the execution details). Each side fit its own ceiling; the honest
    union is ~7 effective lines past develop''s fresh 43-line registration. Same merge-growth class as
    the groups.py exemption; the body shrinks back when the approval section or the skill editor moves
    to its own module.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 55
- check: architecture.file_budget
  path: server/app/workflows/approval_node.py
  reason: 'Both sides of the PR 317 merge extended the approval-node module: develop extracted validate_non_start_fields
    (#284 type integration) and #76 added skill to the forbidden-field and snapshot-placeholder lists.
    The union is ~8 effective lines past the 118 ceiling each side satisfied alone; compressing the module
    docstring to fit would trade the EXEC-APPROVAL-001 contract documentation for budget bookkeeping.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 131
- check: architecture.file_budget
  path: server/app/workflows/builtin_demo.py
  reason: 'The PR 317 merge adds develop''s explicit per-node type fields (#284) and the #76 per-node
    skill pins to the demo DAG — one effective line past the 87 ceiling. The DAG literal is flat data
    where each line is a field; splitting data across modules would hurt readability for no structural
    gain.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/300
  ceiling: 93
- check: architecture.file_budget
  path: server/app/workflow_worker/mark_scan.py
  reason: The issue-211 Phase 3 read-binding batch rekeys the scan state per workspace — the pre-v62 shape
    where several workspaces shared one workflow key collapsed them into a single scan state, and the
    v62 binding (key equals the workspace id) makes that shape unreachable. The overage is the docstring
    documenting the per-workspace rekey, and Phase 4 drops the workflow_key parameter entirely, retiring
    this exemption.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 97
- check: architecture.file_budget
  path: server/app/db/migrations/jobs_workflow_key_alignment.py
  reason: The issue-211 Phase 3 v68 data migration aligns every stored workflow_key with the v62 binding
    across the live tables plus the node_code entity-key prefix. Each composite-unique surface carries
    its collision rule — status-count merge (upsert plus delete), state-table twin drop, revision active-archive
    plus uniform version-shift, and the entity-key published-wins twin rules (twin pass precedes the demotion
    so frozen-pin hashes survive; both-published twins resolve to the window-era row), state-table multi-generation
    dedupe, and the re-prefix separator guard. The issue-211 M2 batch adds the post-v70 shape guard via
    the shared has_column probe (fresh databases already run the terminal shape; each pass skips its table
    instead of failing the replay). The overage is that single transaction's honest cost; M2 dropped the
    workflow_key columns, and M3 (retiring the deprecated contract fields) collapses these migration bodies
    and the exemption with them.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 242
- check: architecture.file_budget
  path: server/app/db/migrations/run_job_status_counts.py
  reason: 'Issue #358''s review round (P2 on #374) adds the dead-row recycle trigger (runs delete purges
    the counter rows): the counter table is deliberately FK-free because jobs.run_id is unconstrained
    text, so the recycle cannot ride a cascade like the v36 workspace twin. Both triggers and the backfill
    are one migration''s cohesive DDL; splitting the file would separate the sync function from its recycle
    pair.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 74
- check: architecture.file_budget
  path: server/app/services/job_list_count_cache.py
  reason: 'Issue #358''s review round (Codex P2 on #374) adds keyed single-flight locking to the TTL cache:
    sync handlers share one service across the FastAPI threadpool, so a cold key under a refresh storm
    ran one full-scan compute per thread. The lock map lives with the entries it guards; splitting it
    out would separate the eviction bound from the map it bounds.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 74
- check: architecture.file_budget
  path: server/app/services/run_partial_failure.py
  reason: 'codex round-1 #2 on PR #479: the two-branch chunked-creation compensation (empty-run
    removal vs partial-run failed-marking) moved into this module so the /runs items path and
    the /job-batches intake share ONE implementation and cannot drift. The error type,
    the operator message, and the compensation protocol are one concern; splitting them across
    modules would separate the failure protocol from its error contract.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/467
  ceiling: 90
- check: architecture.file_budget
  path: server/app/db/migrations/__init__.py
  reason: 'The #358/#359 merge round registers two new migration functions (ops_runtime_profile_samples,
    run_job_status_counts); ruff''s isort normalizes each single-name import with a noqa marker to the
    parenthesized form, so the file grew by two lines against its baseline. The file is a pure re-export
    index whose growth model is one entry per migration module; revisit if the migrations package gains
    a programmatic registry.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 67
- check: architecture.file_budget
  path: server/app/db/migrations/runs_sql.py
  reason: The issue-211 M2 batch adds _INSERT_RUNS_SQL_V70 — the column-less twin of the job_batches cutover
    (without it a pre-v53 database upgraded to v70+ silently drops every historical batch row).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 145
- check: architecture.file_budget
  path: server/app/db/migrations/workspace_execution_defaults.py
  reason: The issue-211 M2 batch adds the revision-key probe and the column-less query twin (pre-v64 databases
    upgraded to v70+ would otherwise fail the v64 migration on the dropped workflow_key column).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 104
- check: architecture.file_budget
  path: server/app/db/migrations/retire_workflow_key_columns.py
  reason: The issue-211 M2 v70 module carries the workflow_key column retirement plus the shared has_column
    shape probe the historical migrations import, and the Codex review on PR 334 (P1/P2) added the upgrade-path
    recreation of the narrowed revision unique constraint and the two same-name workspace-keyed indexes
    the column drop auto-drops (fresh databases already carry them via the schema replay). Goes stale
    when M3 retires the last deprecated contract fields and the probe consumers collapse.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 91
- check: architecture.file_budget
  path: server/app/jobs/queries/batch_queue.py
  reason: The issue-211 M2 batch applies the same deprecated workflow_key identity backfill to the remaining
    runs-row write paths (intake claim, depleted requeue, intake update) so every runs-row consumer inherits
    the field until the 2026-10-31 removal window closes.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/211
  ceiling: 82
- check: architecture.file_budget
  path: frontend/src/features/previewPanel/PreviewPanelHost.tsx
  reason: The Codex P1 review on PR 345 added the host-injected CSP (the outbound-network red line (sandbox
    allow-scripts does not block fetch/sendBeacon/img to external origins)). The policy constant, the
    srcDoc head-injection helper and its documentation grow the security surface in place -- splitting
    them out would separate the CSP from the iframe that enforces it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/347
  ceiling: 199
- check: architecture.file_budget
  path: frontend/src/features/previewPanel/CustomizePreviewDialog.tsx
  reason: '#347 P1: draft execution became an explicit per-session action (preview-draft gating). The
    dialog footer gained the "预览此草稿" button with its previewing state plus the previewDraft/onPreviewDraft
    contract toward PreviewPanelSection; the gating state itself lives in the section (single source,
    dialog stays the thin chat wrapper). The added lines are the honest cost of the explicit consent
    UI; re-evaluate when the preview-panel governance UI is next restructured (#347 follow-up).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/347
  ceiling: 173
- check: architecture.file_budget
  path: frontend/src/features/previewPanel/PreviewPanelSection.tsx
  reason: The Codex P2 review on PR 345 added bundle content into the PreviewPanelHost remount key plus
    the hashBundle helper -- a cross-navigation bridge-request isolation fix that must stay at the composition
    site to keep the in-flight-window reasoning local.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 79
- check: architecture.file_budget
  path: server/app/executors/_code_sandbox.py
  reason: '#383: the Host-side sandbox probe switched from shutil.which("velites") to the shared resolve_sandbox_binary
    primitive (velites-sandbox -> velites candidates) and the fail-closed error text now names the docker
    host form''s semantics (code local fallback disabled — running code nodes on a Worker is the supported
    path) and the bare-metal remedy.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 207
- check: architecture.file_budget
  path: server/app/routes/agent_workers.py
  reason: '#381 version handshake: the register endpoint logs the reported runtime_versions mapping (worker
    code x velites version matrix) at registration time. Drift diagnostics for externally-mounted runtimes;
    informational only, no admission change. #490 adds the structured register events at the same endpoint
    (worker.registered after the transaction, worker.register_rejected at the 400/401 exits — one call each,
    payloads built in worker_events.py); the ceiling covers both generations.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 236
- check: architecture.file_budget
  path: shared/code_sandbox.py
  reason: '#383 added the sandbox-binary resolution primitive here (candidates velites-sandbox -> velites;
    bundled data/bin dir then PATH) because the module is already the stdlib-only shared home both the
    Host executor and the Worker code runner import. The primitive keeps one probe surface for both sides
    so "preflight passes = runtime resolves the same wrapper"; BUNDLED_SANDBOX_DIR is re-exported by worker/binary_resolution.py
    as the single source of truth for the shared directory.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 141
- check: architecture.file_budget
  path: worker/runtime/catalog.py
  reason: '#381 version handshake: probe_runtime_versions asks each effective runtime for its --version
    output so the registration payload carries the "worker code x velites version" compatibility matrix
    (velites is independently versioned once externally mounted; drift becomes observable instead of a
    human upgrade discipline). Purely additive function beside the detection code it complements; unknown
    results record "<unknown>" by design (observability, not a fail-closed guard).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 90
- check: architecture.file_budget
  path: worker/runtime/preflight.py
  reason: '#381 moved velites/pi out of the worker image (runtime executors are externally-mounted binaries
    now), which turned "forgot to mount" from a rare deployment accident into a routine mistake with a
    silent failure mode — auto-detection yields an empty set and the worker registers with zero capacity.
    The expect-runtimes guard added here (env parsing, missing-detection against the runtime catalog,
    actionable error text covering the bundled dir and PATH) doubles the file past its first ceiling.
    The module stays a single cohesive concern (startup preconditions for the executor); splitting the
    guard into its own module was rejected because preflight_error would reduce to a shell over two helpers.
    Revisit the split if preflight grows a third guard.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 80
- check: architecture.file_budget
  path: worker/runtime/setup.py
  reason: '#381 wired AGENT_WORKER_EXPECT_RUNTIMES through prepare_runtime_models (env read + ValueError
    mapping into the shared startup-error string) alongside the existing resolve_config_runtimes handling,
    and the codex review on #384 extended it: model-discovery failures for an *expected* runtime now hard-fail
    startup (a wrong-arch binary passes the existence probe and only breaks at exec time), while unexpected
    runtimes keep the soft warning. This module is the composition point between config parsing, preflight,
    and model discovery; there is no meaningful split for the guard wiring.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 62
- check: architecture.file_budget
  path: server/app/services/preview_panels.py
  reason: The Codex P2 review on PR 345 unified the artifact inventory on _merged_artifact_names so every
    recent job merges the object-store manifest like the selected job does (the object store is the authoritative
    copy, the local dir an evictable cache). Single-purpose helper next to its only consumer.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 190
- check: architecture.file_budget
  path: server/app/services/s3_jobs_gc.py
  reason: 'Re-filed (systematic review round, PR #344): the S3 jobs GC service (#340) is a single scan-decide-delete
    pipeline. It grew past its first exemption (167) on two correctness rounds that both belong with the
    pipeline they protect: the TOCTOU fix (per-batch fresh DB revalidation before deletes — a concurrent
    promote can recreate a scanned orphan key between scan and delete, and deleting it would leave the
    manifest pointing at missing content) and the materials-collision fix (material keys of a workspace
    literally named jobs / jobs-staging fall inside both GC prefixes; the staging pass originally had
    no DB check at all, so both passes now go through the key-existence seam and staging gained its own
    orphan filter). Splitting either fix out would separate the batch-boundary discipline from the delete
    batching it guards; revisit if a second consumer of the revalidate seam appears.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/340
  ceiling: 200
- check: architecture.file_budget
  path: server/app/jobs/queries/job_artifact_keys.py
  reason: 'Re-filed (systematic review round, PR #344): the GC storage-key query surface for job_artifacts
    and materials lives in one mixin (#340) — the systematic review found that material keys of a workspace
    literally named jobs / jobs-staging land inside the GC prefixes, so both the scan-time loader and
    the delete-time revalidation lookup had to union materials.storage_key; splitting the two tables across
    mixins would separate one reference set into two call sites that must stay in lockstep.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/340
  ceiling: 47
- check: architecture.file_budget
  path: server/app/agent_broker/agent_result_commit.py
  reason: 'Issue #359 adds the execute-done counter at the terminal-commit point (two effective lines).
    The counter must fire exactly where the lease and request both reach their terminal state — moving
    it out would separate the gauge from the state transition it measures.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 72
- check: architecture.file_budget
  path: server/app/agent_broker/dispatch_pool.py
  reason: 'Issue #359 counts submissions and saturation skips in submit() plus a pending_depth() reader
    for the sampler. The counters must sit at the queue boundary they observe; the pool module is already
    the smallest unit that owns the backlog semantics.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 60
- check: architecture.file_budget
  path: server/app/services/runtime_profile/classifier.py
  reason: 'Issue #359''s P1 review round added the blocked-claim branch (the #351 discrimination table''s
    blocked row: deep queue + workers claiming + every candidate skipped). The branch shares the evidence
    dict and the verdict helpers with its sibling rules; splitting the classifier by rule family would
    separate the priority order (documented in the module docstring) from the code that implements it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 145
- check: architecture.file_budget
  path: server/app/services/ops_metrics/service.py
  reason: 'Issue #359 extends the sampling pass to persist the runtime-profile gauge row (one call plus
    the pool/pool-backlog gauge readers kept next to their only consumer). The sampler stays one cohesive
    write surface; the durable split is a sampling-dispatch module if more gauge families join, at which
    point this exemption goes stale. #490 feeds the offline detector from the same last_seen read (one
    call, threshold restored to match the online count — review P0 on #494).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 230
- check: architecture.file_budget
  path: server/app/routes/__init__.py
  reason: Issue 329 mounts the job diagnosis observation router in the route assembly (one import + one
    secured() call, three effective lines past the baseline ceiling 137). The file is the single mount
    point for every feature router; the durable fix is splitting the assembly per domain, at which point
    this exemption goes stale.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 140
- check: architecture.file_budget
  path: server/app/events/buffer.py
  reason: Issue 353 swaps the per-event job_event_seq bump for segmented allocation (one UPDATE grants
    SEGMENT_SIZE revisions, spent from an in-process counter). The growth is a module-level constant with
    its trade-off docstring (segment size rationale, watermark-not-dense-index consumer contract, multi-replica
    reorder window) plus the segment window arithmetic in _next_revision_locked — all of it must stay
    next to the issuer it governs; splitting the allocator out would separate the deque-order invariant
    from the lock that enforces it. Revisit when the events package grows a second revision consumer.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/349
  ceiling: 130
- check: architecture.file_budget
  path: server/app/agent_broker/reaper.py
  reason: 'Issue 357 persists the reap watermark: the first pass after a restart restores it from global_settings
    (ReapWatermarkStore) instead of rescanning all terminal rows, and every completed pass saves the advance
    (5 effective lines past the baseline ceiling 66). The restore and the save sit exactly at the watermark''s
    two transition points, so moving them to a helper would separate the cursor semantics from the scan
    they gate; the durable fix is merging the two query branches into the shared store module when the
    next reaper change touches it, at which point this exemption goes stale.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 81
- check: architecture.file_budget
  path: server/app/services/studio_publish_requests.py
  reason: 'The #429 review rounds harden the confirm path: resolve drops the expires_at predicate (a publish
    that outlives the remaining TTL must still record confirmed — TOCTOU), a losing resolve race reports
    the row''s final state instead of 404, the claim does the TTL check and the observed-expiry sweep,
    and result_revision_id distinguishes a new revision from a runtime-only save via active-before/after
    probing. The #429 third round (codex P1s) adds the confirming claim state (the pre-publish atomic
    pending→confirming move that closes the cancel race and rolls back to pending on refused gates) and
    the confirm-window re-request refusal; the shared helpers (wire payload, expiry comparison, the draft-version
    token and its stale-claim refusal) were split into studio_publish_request_support.py to keep this
    file within its ceiling. The fourth round (#429 四轮) splits the poll-side read (pending/live-confirming
    surfacing, the stale-claim sweep) into studio_publish_request_poll.py to stay within the ceiling.
    Revisit when the handshake grows its next change (e.g. the deferred chat_session_id wiring).'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 199
- check: architecture.file_budget
  path: server/app/jobs/queries/studio_publish_requests.py
  reason: 'The #429 fix reworks lazy expiry for the 5s poll: the pending read becomes pure (no write connection
    per poll), the observed-expiry write moves to expire_pending_publish_request, resolve loses its expires_at
    predicate, and supersede_pending_publish_requests serves the manual publish path. The state-transition
    statements share one module by design (the handshake''s single row-lifecycle in one file); the growth
    is the transitions plus their concurrency-contract docstrings. The second-round review removed the
    dead _sweep_expired_publish_requests helper (zero callers), lowering the ceiling from 186 to 184 (#429).
    The third round (#429 三轮) splits the confirm-race transitions (claim/resolve/reject/confirming read)
    into studio_publish_request_claims.py — this module keeps create / supersede / lazy expiry. The fourth
    round (#429 四轮) moves the reads into studio_publish_request_reads.py and pulls the confirming guard
    into create''s transaction (advisory-lock-serialized against the claim, so the guard-supersede-INSERT
    triple is atomic). Revisit when issue 416''s follow-ups (chat_session_id wiring) touch it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 184
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useAgentPublishRequest.ts
  reason: 'The #429 fix moves the resolved notice into a shared zustand store (per-instance useState was
    a dead feature — the dialog''s confirm/cancel wrote a notice the aside instance could never read)
    and observes the pending→null transition so an externally-superseded request (manual publish / newer
    agent request) lands a dismissable receipt instead of the dialog closing silently. The observation
    effect and the two refs belong with the confirm/cancel actions they disambiguate; the notice text
    builders already live in agentPublishNoticeStore. The fourth round (#429 四轮) adds publishInFlight
    (the whole-confirm window flag the dialog uses to show "publish in progress" on polled confirming
    rows) and teaches the observation effect to skip confirming rows (a live claim is not a terminal null
    jump — no bogus receipt). The closing round (#429 收尾 P3) makes a cancel 404 silent (the backend''s
    deliberate lost-to-the-claim answer is not an operation failure; the row state speaks for itself)
    while non-404 failures keep the toast. Revisit when issue 416''s deferred follow-ups (buffered rejection,
    session wiring) rework this hook.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 137
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/AgentPublishRequestDialog.tsx
  reason: 'The #429 fourth round (#429 四轮 P2-1/P3-2) makes the confirm''s draft flush a completion-order
    await (flushDraftSave returns the PUT promise; the server-draft re-read and the confirm endpoint fire
    only after it resolves — the old fire-and-forget could publish a stale draft whose hash happened to
    match) and teaches the dialog to hold itself open on polled confirming rows (publishInFlight keeps
    the buttons disabled mid-publish instead of the dialog vanishing). The growth is the try/catch around
    the flush/read-draft chain plus the error toast the review asked for. Revisit when the deferred follow-ups
    (a shared useConfirmPublish hook) rework the dialog''s action wiring.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 59
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/StudioChatAside.tsx
  reason: 'The #429 fix makes the resolved notice real: it renders from the shared store and is dismissable
    (a close button next to the message). The growth is the dismissable-notice markup; the aside stays
    the single place the receipt is displayed. Revisit when issue 416''s deferred follow-ups rework the
    aside layout.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 56
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/chat/StudioChatPanel.module.css
  reason: 'The #429 fix adds scopeNoteDismiss (the resolved-notice close button) and flexes scopeNote
    to host it; the module is a flat stylesheet at the repo''s per-root CSS limit policy, and the 7-line
    growth is one rule plus its comment. Revisit at the next stylesheet cleanup pass.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 362
- check: architecture.file_budget
  path: server/app/db/migrations/studio_publish_requests.py
  reason: 'The #429 third round (codex P1-1/P1-2) hardens the v75 DDL itself: the partial unique index
    that makes one-pending-row-per-workspace a database invariant (the concurrent-create loser retries
    instead of leaving a second pending row), the ``confirming`` claim state in the status check (the
    cancel race closes at the schema level), and the ``draft_hash`` column (the confirm binds to the draft
    the agent requested). The migration is unmerged (#429) so the growth rides the same review; the state-machine
    and index-contract docstrings are the load-bearing part of the change — the DDL without them re-opens
    the exact races they document. The fourth round (#429 四轮) adds the ``claimed_at`` column (the stale-claim
    sweep''s clock for a process that died between claim and resolve). The rebase round (#434) renumbers
    the module v75→v76 (#427''s node_runs_skill_key claimed 75) — two docstring lines for the renumber
    provenance. Revisit when the migration lands or the handshake''s next schema change reworks it.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 70
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/draftSaveController.ts
  reason: '#429 rounds four and closing: flushNow/save return the PUT promise the agent-publish confirm
    awaits (completion order — the re-read and the confirm endpoint fire only after the draft lands),
    and the closing round makes the resolved value carry the flush''s terminal result ({ok, state} — the
    controller resolves on every path and failure lives only in state, so a React-snapshot guard would
    miss a PUT that fails during the await). The result type plus its contract docstrings are the load-bearing
    concurrency narrative; splitting them from the state machine would separate the completion-order rules
    from the code they govern.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 171
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/useWorkflowDraftPersistence.ts
  reason: '#429 closing P2-1: flushNow now returns the DraftSaveFlushResult (the live terminal state,
    not the React useState snapshot the dialog''s closure captured at click time — the snapshot guard
    missed a PUT failing during the await and would publish the stale draft). The unhydrated no-op branch
    resolves {ok: true, IDLE} so awaiting callers treat it as no-content-no-failure. The typing/return-shape
    growth rides the same review round; revisit when the draft-save wiring is next reworked.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 95
- check: architecture.file_budget
  path: frontend/src/lib/queryKeysExtra.ts
  reason: 'Merge-growth class (PR 317 precedent): develop''s #419 connectionKeys and #427 nodeRuns entries
    plus this branch''s studioPublishRequest key (the agent publish request poll, #416) union to one effective
    line past the 65 ceiling each side satisfied alone. The module is a flat key registry; each line is
    one cache identity. Revisit at the next registry cleanup.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 66
- check: architecture.file_budget
  path: server/app/db/connection.py
  reason: '#438 makes __exit__ leak-proof (close in finally so a failed commit/rollback cannot strand the
    checkout) and stamps the checkout origin for the pool return-path telemetry. The 8 effective lines are
    the two documented guards; the attribution logic itself lives in pool_reset.py. Stays until the
    transaction helpers retire this facade.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 80
- check: architecture.file_budget
  path: server/app/db/pools.py
  reason: '#438 installs the pool reset callback (reset=reset_connection) so every returned connection is
    verified IDLE and dirty returns are attributed — one effective line plus wiring comment.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 60
- check: architecture.file_budget
  path: server/app/db/pool_reset.py
  reason: '#439 hardens the leak telemetry this file exists for: the frame walk gets a 30-frame hard cap
    (the collected-frames bound alone did not bound the walk over plumbing-only stacks), the 60s rate limit
    gets a suppressed-hit counter so the next warning reports leak frequency, and past-cap origins degrade
    to an explicit walk-cap marker. 101 effective lines; revisit when the attribution walk moves to a
    sampled/only-on-leak model.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 106
- check: architecture.file_budget
  path: server/app/db/transaction.py
  reason: '#438 removes the double-BEGIN (root cause of the warning storm) and adds _rollback_quietly so a
    failing rollback on a broken connection cannot skip close() in the finally paths. Growth is the guarded
    rollback helper plus its rationale comments. #439 splits the guard into _rollback_quietly (now WARNs
    instead of silencing: a rollback that cannot run means a broken connection nothing else reports) and
    _release_quietly (close() in finally must not replace the caller''s original exception), inlined within
    the frozen ceiling.'
  owner: database
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/456
  ceiling: 61
- check: architecture.file_budget
  path: server/app/workflows/loader.py
  reason: >-
    Issue #458 round-trip fix: the asdict snapshot reloader must translate
    the serialized reduce.from_node spelling back to the yaml from the
    loader reads (mirroring the existing node_type translation), else every
    sharded/reduce revision snapshot fails reparse with "reduce.from is
    required". A ten-line guarded translation at the one dict->definition
    boundary; moving it out would fork the snapshot normalization the
    loader already owns. Revisit when snapshot loading gains its own module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/458
  ceiling: 309
- check: architecture.file_budget
  path: server/app/workflows/revision_format.py
  reason: '#458 grows definition_to_yaml past its 131 ceiling: the echo must carry the shard/reduce
    declarations (previously dropped — the studio''s initial draft YAML showed a ghost change against
    its own baseline and publishing it silently stripped fan-out) plus the apply_shard_echo helper
    extracted to keep the non-default-keys spelling next to its loader counterpart. The alternative,
    splitting the echo out of the revision format module, would fork the one serializer every
    revision/job-snapshot path shares. Remove when the revision echo moves to a schema-driven
    serializer or the file gets a structural split of its own.'
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/458
  ceiling: 157
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/validation/workflowStudioChangeFormatters.ts
  reason: >-
    Issue #431 adds the remaining compare-field labels (node_type, after,
    shard, reduce) so the changes drawer never shows raw field names for
    the fields compare now diffs; the file is a flat label table where
    every line is a translation. Revisit when the label table moves to the
    generated change-contract types or gets i18n tooling.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/431
  ceiling: 88
- check: architecture.file_budget
  path: velites/src/events.rs
  reason: >-
    Issue #469 adds ToolTiming to the event schema module: the phase
    struct (spawnMs / firstByteMs / restMs / reapMs with per-phase wire
    docs, including the firstByteMs observation semantics), the
    tool_execution_end wiring, and the wire-shape tests sit next to every
    other event type's definition and contract test — the module IS the
    event schema. The growth is docstring + test lines over a ~50-line
    struct; dropping the docs to fit would trade the phase semantics
    (which phase covers what, when absent) for bookkeeping. Ceiling at
    actual + 10 per the dated-exemption convention (#431 / #458
    precedents). Remove when the timing fields move into a schema-driven
    shape or the module gets a dedicated events-tools split. #577 moved
    StdoutJsonlSink out to velites/src/event_sink.rs; ceiling re-seated
    at the post-move actual + 10.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 494
- check: architecture.file_budget
  path: velites/src/tools/bash.rs
  reason: >-
    Issue #469 instruments the bash tool with the phase decomposition the
    stall forensics asked for: spawn timing, first-output-byte reading
    (read_with_first_byte replaces read_to_end with identical byte/error
    semantics; the first byte is the earlier of the two streams), the
    reap-phase branch, and the module docstring documenting the
    observation axes and the harness-read-vs-child-write caveat. The tool
    is one cohesive spawn/read/terminate flow; splitting instrumentation
    out would fork the child lifecycle across modules. Ceiling at actual +
    10 per the dated-exemption convention (#431 / #458 precedents) — the
    module is in active observation tuning. Remove when #469's follow-up
    (once the write() stall trigger is pinned) retires or reshapes the
    instrumentation.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 267
- check: architecture.file_budget
  path: velites/src/tools/mod.rs
  reason: >-
    Issue #469 review fix makes the ToolKind::execute dispatch boundary
    the single totalMs timing point (every tool kind gets it; the
    in-process tools carry zero instrumentation of their own — the uuid
    and validate exemptions retired when their timing code was deleted).
    mod.rs is the tools facade every tool already imports. Ceiling at
    actual + 10 per the dated-exemption convention (#431 / #458
    precedents). Remove when the file shrinks back under its old ceiling.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/469
  ceiling: 159
- check: architecture.file_budget
  path: server/app/config_schema.py
  reason: >-
    codex P1 on #432: a "secret: true" property declaring a plaintext
    "default" is the fourth vault bypass — the schema-default merge
    (config_schema_defaults) freezes the credential verbatim into every
    intake snapshot even when no layer ever sets the field. Rejecting it
    lives in _validate_property (2 code lines + comment), the single
    chokepoint every declaration channel already calls: Agent definitions,
    node config_schema blocks, executor contracts. Splitting the validator
    to dodge 1 line of growth would fork the schema subset's one
    well-formedness gate. Remove when the schema validator gets its own
    structural split or #432's secret governance consolidates.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/432
  ceiling: 161
- check: architecture.file_budget
  path: server/app/services/node_config_secret_guard.py
  reason: >-
    codex P1 on #432: the guard gains the secret+default gate —
    reject_secret_schema_defaults re-raises the shared validator verdict
    with the vault-channel guide (SECRET_DEFAULT_GUIDE) so resolve/publish
    errors point at the fix, and secret_gate_errors calls it per executable
    draft node so publish fails before a revision can strand intake. The
    previous 71 ceiling left 10 effective lines of headroom; the gate plus
    its channel-pointer comment needs 7 more. Remove when the #432 guard
    family gets its structural split or the error-guide texts move to a
    shared errors module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/432
  ceiling: 88
- check: architecture.file_budget
  path: server/app/workflow_worker/code_stock.py
  reason: >-
    Issue #401 review P1-1 adds pass_budget to the code stock gate: the
    v79 shard-aware index removed the accidental one-in-flight backpressure
    on shard fan-outs, and within one poll pass the gate's queued count
    cannot see the shards just submitted to the asynchronous enqueue pool
    (it counts only committed rows). The shard fan-out loop now caps its
    own submissions at the gate's remaining budget — same target, clamps
    and TTL as allows(), extracted into a shared _ensure_fresh so the two
    entry points cannot drift. ~5 effective lines; splitting the gate
    module for one accessor would fork the single stockpile discipline.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/401
  ceiling: 75
- check: architecture.file_budget
  path: shared/protocol.py
  reason: >-
    Issue #352 adds protocol v5 (per-Worker batch heartbeat) to the version
    history: this module is the single source of truth both sides import (the
    worker image ships shared/), and each version entry must state its
    mixed-fleet contract for the next bump. Splitting the history out would
    fork the one file both sides already share. Revisit when the version
    history moves to generated docs.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/352
  ceiling: 43
- check: architecture.file_budget
  path: worker/execution/heartbeat.py
  reason: >-
    Issue #352 turns the per-execution heartbeat thread starter into a
    facade over the per-Worker batch registry: ExecutionHeartbeat keeps the
    executor/upload call shape (adopt/shutdown/quiesce/resume/proc_ref) so
    run.py and code_runner.py stay untouched by the threading change, while
    each method forwards to the registry. The four one-line forwards plus
    the degraded-mode contract docstring are the honest cost of that
    compatibility seam. Remove when the no-registry single-beat mode retires
    and only the batch registry path remains. Ceiling re-dated 75 to 79 for
    the #497 codex P1: lease-scoped registry mutations (prune/quiesce/
    resume/set_adopted) match on (execution_id, lease_id) so an old
    attempt's late cleanup cannot drop or silence the entry of a re-claimed
    new attempt — the facade threads its lease_id through the forwards.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/352
  ceiling: 79
- check: architecture.file_budget
  path: worker/executor.py
  reason: >-
    Issue #352 starts the per-Worker batch heartbeat coordinator in main():
    three lines (import, the coordinator start wiring the upload queue to
    the same registry, the registry argument on the run_execution submit).
    The executor main is the single place where the machine-wide stop event
    and the UploadQueue live together; moving the wiring out would fork
    lifecycle ownership. Revisit when executor main gets its next split.
    Ceiling re-dated 226 to 224 after the rebase onto 0.7.0 (#481
    ClaimPacing): #481's import cleanup (agent_subprocess_env re-export
    retired, its test consumers import worker.execution.run directly) left
    214 effective lines with this wiring on top, so the ceiling realigns to
    actual + 10 per the dated-exemption convention (#431 / #458 precedents).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/352
  ceiling: 224
- check: architecture.file_budget
  path: worker/claim_budget.py
  reason: >-
    Issue #534 splits the per-pool claim-pass budget out of executor.py
    (its 224 ceiling): pass_budget owns how the two pools' budgets and
    capacity declarations derive from effective capacity, active
    executions, upload backpressure, and the #534 cross-pool deferral.
    PR #539 review hardening (P2-2/P3: explicit mutate contract in the
    module docstring, Future-typed parameters, the held⟺avail==0 note on
    the budget expression) added 4 effective lines over the first
    registration; ceiling = actual + 10 per the dated-exemption convention
    (#431 / #458 precedents).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/534
  ceiling: 67
- check: architecture.file_budget
  path: worker/service_models.py
  reason: >-
    #546 wires claim_batch_limit (the batch claim size knob) into the Worker
    config API: one optional field on WorkerConfigPayload (+1 effective
    line). The file is a flat payload-model list with nothing left to split;
    the exemption closes when the payload models are next split by domain
    (tracked by the remove_when issue).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/549
  ceiling: 34
- check: architecture.file_budget
  path: worker/config_validation.py
  reason: >-
    #546 wires claim_batch_limit into the Worker config surface:
    _EDITABLE_FIELDS + _DEFAULTS entries plus the value validation aligned
    with max_code_concurrency's in-file check (+3 effective lines). Same
    shape as the existing one-field precedents; the exemption closes when
    per-field validation moves out to the field-owning modules (tracked by
    the remove_when issue).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/549
  ceiling: 148
- check: architecture.file_budget
  path: worker/upload/heartbeat.py
  reason: >-
    Issue #352 routes the upload-side lease heartbeat through the per-Worker
    batch registry: start_upload_heartbeat takes the task (registry mode
    registers the lease, no-registry mode keeps the single-beat thread for
    unit tests), prune/quiesce forward to the registry. The dual-mode
    docstrings are load-bearing: the single-beat path is the degraded
    pre-v5-Host contract. Remove when the no-registry single-beat mode
    retires and only the batch registry path remains. Ceiling re-dated 66
    to 68 for the #497 codex P1: prune/quiesce pair-match on the task's own
    (execution_id, lease_id) — a re-claimed execution's entry must survive
    an old attempt's final stop.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/352
  ceiling: 68
- check: architecture.file_budget
  path: worker/execution/heartbeat_batch.py
  reason: >-
    Issue #352 review fixes grow the per-Worker batch heartbeat coordinator
    past its first-check-in ceiling: lease sharding at MAX_BATCH_HEARTBEATS
    (a chunk's transient error aborts the tick — never a degrade; only
    404/405 flips the mode — and the next tick retries from the top,
    per-item Host predicates being idempotent), the degraded single-beat
    path carrying SINGLE_BEAT_TIMEOUT_SECONDS so one slow Host response
    cannot serially starve every other lease, and the facade-shared
    proc_ref wiring (zombie stop must read the same dict the executor
    writes). All three are core loop semantics, not splittable helpers.
    Revisit when the coordinator gains its own degraded-mode or sharding
    module. Ceiling re-dated 238 to 274 for the #497 codex round: the two
    P1s — lease-scoped registry mutations pair-matched on
    (execution_id, lease_id) via the _matching_entry guard, and the
    degraded single-beat path riding one short-lived daemon thread per
    lease (the serial N × timeout sum could cross the lease TTL) — are
    both core loop semantics living next to the loops they fix.
    Ceiling re-dated 274 to 271 for #566 phase 2: the degraded single-beat
    path moved to heartbeat_degraded.py (the split this exemption's own
    revisit note nominated), making room for the registry's
    apply_beat_result (the supervisor relay's pair-matched write-back).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/352
  ceiling: 271
- check: architecture.file_budget
  path: server/app/agent_broker/claim_scan.py
  reason: >-
    #498 (PR #497 review) adds the ClaimOutcome frozen dataclass here —
    the post-transaction event payload the broker emits only after the
    claim transaction commits. It lives beside AgentClaim/WorkerView/
    ScanState because it is the same family of per-claim-pass value
    objects (the scan modules already own the claim data model), and
    giving it its own module would split the claim's data model across
    files for ~26 lines. Remove when the claim path's next rework
    re-budgets the family together with claim.py's entry.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/498
  ceiling: 151
- check: architecture.file_budget
  path: server/app/agent_broker/heartbeat_single.py
  reason: >-
    #499 (PR #497 review) applies the #498 post-commit event discipline to
    the single heartbeat: the refusal reason is decided inside the write
    transaction (_renew_in_transaction, None = renewed) and the
    execution.heartbeat_rejected event fires only after the commit
    succeeds, sharing the HEARTBEAT_* reason literals with the batch
    path. The split (transaction inner function + post-commit emit in the
    public wrapper) is the minimal shape that keeps the verdict and its
    emission in one readable flow; the alternative — emitting in place —
    is exactly the ghost-event bug this fixes. Remove when the heartbeat
    paths gain their own shared renewal module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/499
  ceiling: 61
- check: architecture.file_budget
  path: server/app/agent_broker/heartbeat_batch.py
  reason: >-
    #499 (PR #497 review) makes the batch path emit one
    execution.heartbeat_rejected per lost item — _renew_one returns the
    refusal reason (shared HEARTBEAT_* literals) instead of a bare bool,
    the lost loop collects reasons inside the transaction, and the
    emissions run only after the batch transaction commits (the #498
    discipline). All three additions are the v5 path's own event
    contract; splitting them out would fork the single/batch pairing the
    shared literals exist to keep aligned. Ceiling re-dated 91 to 98 for
    PR #497 review round #5125358408 P1-B: the renewal loop takes its
    per-item row locks in sorted execution_id order (two concurrent
    batches sharing one worker registration can no longer interleave
    A,B / B,A) and the lock-order note now documents the real three-table
    lock surface (AER → executor_leases → agent_workers, same direction
    as every other renewal path). PR base is develop, where this file
    does not exist. Remove when the heartbeat paths gain their own
    shared renewal module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/499
  ceiling: 98
- check: architecture.file_budget
  path: worker/host/heartbeat_ops.py
  reason: >-
    PR #497 review round #5125358408 P2: the batch endpoint's 404/405
    branch logs one INFO line carrying the lease count — degraded to
    single beats, N leases × SINGLE_BEAT_TIMEOUT_SECONDS is the per-tick
    wall-clock ceiling of the rolling-upgrade window, and the transition
    was previously visible only as a count-free mode flip. One logging
    call plus its module logger; remove together with the heartbeat_ops
    split when the client gains a structured-events side.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/499
  ceiling: 83
- check: architecture.file_budget
  path: server/app/agent_runtime/adapter.py
  reason: >-
    #476 adds the tool-catalog half to the runtime adapter contract:
    ToolCatalogEntry (tier/activation semantics) and the tool_catalog
    field on RuntimeAdapter. The two contract halves (execution keys,
    tool catalog) belong in one file — every consumer (dispatch
    validation, claim, the Studio API projection) resolves them through
    the same adapter object. Remove when a runtime contract split
    becomes warranted by a third contract family.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 62
- check: architecture.file_budget
  path: server/app/agent_runtime/velites.py
  reason: >-
    #476 wires the velites static tool-catalog mirror (the
    `velites tools list --json` reflection pinned by the cross-binary
    contract test) into the adapter declaration — one dataclass field.
    The mirror itself lives in tool_catalog.py; this file only carries
    the reference. Remove together with the mirror if velites ever
    self-describes over the wire instead of via static declaration.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 19
- check: architecture.file_budget
  path: server/app/agent_runtime/pi.py
  reason: >-
    #476 registers pi's static tool catalog (external runtime, no
    self-description channel — names-only triple by design). One
    import plus one adapter field; the argv builder below it is
    untouched. Remove if pi gains a self-description command or the
    adapter drops per-runtime tool declarations.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 59
- check: architecture.file_budget
  path: server/app/agent_broker/dispatch.py
  reason: >-
    #449/#476 turn the manifest tools freeze from a bare list copy into
    a catalog-validated call (manifest_tools) — one import plus one
    call site replacing the previous one-liner. The validation logic
    lives in agent_runtime/tools_validation.py; dispatch keeps only the
    freeze point. Remove when the manifest assembly is extracted into
    its own module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 136
- check: architecture.file_budget
  path: velites/src/main.rs
  reason: >-
    #476 dispatches the `tools list` token before the agent-run CLI
    parse — the same pre-parse dispatch block as `models list` /
    `sandbox wrap` / `validate` (that CLI requires a positional, so
    subcommands must be peeled off first). The handler itself is one
    catalog call in tools/catalog.rs. Remove if the pre-parse dispatch
    moves into a shared argv router.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 85
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/AgentEditor.tsx
  reason: >-
    #476 makes the tool picker catalog-driven: option-surface derivation
    (selectable/forced/invalid sets as memoized maps), the forced-tier
    locked-row hint, and the runtime-switch invalid-tool banner join the
    existing form lifecycle. The catalog plumbing is already split out
    (useAgentRuntimes hook); what stays here is the interaction itself —
    splitting render from state would separate the two halves of every
    conditional being tested. Remove when the editor gains a shared
    tool-picker component with WorkflowNodeToolsEditor.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 359
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/workflowStudioYamlDraft.ts
  reason: >-
    #443/#476 add patchWorkflowNodeTools — one patchNode call site with the
    empty-means-undeclared delete-key semantics, same shape as the sibling
    patch functions in this table. Remove together with the node-level
    tools declaration if it ever retires.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 123
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/shared/workflowStudioYamlDraft.parse.ts
  reason: >-
    #443: WorkflowYamlNode gains the optional tools field (one line plus
    its contract comment). The node shape lives in one type by design.
    Remove with the field itself.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 85
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowAgentExecutionDetails.tsx
  reason: >-
    #443/#476 mount WorkflowNodeToolsEditor below the runtime settings —
    one import plus one JSX block (9 lines) in the agent execution body.
    The editor itself is a sibling component; this file is only the
    composition point. Remove if the composition is restructured.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 65
- check: architecture.file_budget
  path: velites/src/tools/specs.rs
  reason: >-
    #518 adds the json tool's provider spec (description + JSON Schema
    parameters) — the file is the per-tool spec table by design, one arm
    per ToolKind; the spec IS the content, there is nothing to split
    without separating a tool's spec from its siblings' in a way no
    consumer wants. Remove only with the json tool itself.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/518
  ceiling: 116
- check: architecture.file_budget
  path: server/app/agent_runtime/tool_catalog.py
  reason: >-
    #518 adds the json tool's entry to the velites static mirror — the
    file is the declared mirror of `velites tools list --json`, pinned
    equal by the cross-binary contract test; its size is the catalog's
    size plus the pi triple. The entry must live here in full
    (description/parameters verbatim) or the parity test fails by
    design. Remove if the mirror is replaced by runtime probing.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/518
  ceiling: 208
- check: architecture.file_budget
  path: velites/src/lib.rs
  reason: >-
    #476/#518: lib.rs carries the forced-tool activation at the one point
    where all three inputs exist (parsed --tools, canonicalized skill
    dirs, --require-output) and the tools-listing error message that
    must name the full tool set. The activation is a harness
    run-assembly concern, not a tool-module one. Remove if the run
    assembler is extracted from the provider wiring.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/518
  ceiling: 211
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodePromptEditor.tsx
  reason: >-
    #513 adds the prompt-mode selector (append/overwrite) to the custom
    prompt editor: one TextField select with two labeled options plus the
    state-switching hint line — the interaction the whole feature is about.
    The data layer lives in useNodePromptPreview; splitting the selector
    out would separate the mode from the textarea it governs. Remove if
    the prompt panel is restructured.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/513
  ceiling: 84
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/useNodePromptPreview.ts
  reason: >-
    #513: the hook derives the prompt mode from the draft YAML (junk-tolerant
    normalization mirroring the prompt field) and exposes onPatchMode —
    data-layer counterparts of the editor's mode selector. Same P1
    normalization family as customPrompt above it. Remove with the mode
    selector.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/513
  ceiling: 100
- check: architecture.file_budget
  path: server/app/agent_broker/agent_claim_compatibility.py
  reason: >-
    #513: the claim-side re-resolution chain carries prompt_mode alongside
    additional_prompt — one line keeping claim-time rebuilds byte-equal
    with dispatch-time manifests (EXEC-RUNTIME-DISPATCH-001 symmetry).
    Remove if the manifest prompt fields centralize into one builder.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/513
  ceiling: 58
- check: architecture.file_budget
  path: server/app/services/node_prompt_preview.py
  reason: >-
    #513: the preview payload gains prompt_mode (normalized to append) and
    save_node_prompt accepts it — the API surface of the mode feature. The
    service is the single preview/save implementation shared by the Studio
    route and the MCP tools; the growth is the feature, not drift. Remove
    if prompt previewing is extracted from the draft-editing service.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/513
  ceiling: 183
- check: architecture.file_budget
  path: server/app/workflows/workflow_execution_defaults.py
  reason: >-
    #513: prompt_mode is node-level-only like prompt — the top-level loader
    rejects it (one guard) and the merge preserves it (one field passthru).
    Same shape as the prompt guards right above. Remove with prompt_mode.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/513
  ceiling: 76
- check: architecture.file_budget
  path: frontend/src/features/workflowStudio/inspector/WorkflowNodeToolsEditor.tsx
  reason: >-
    codex P2 on #527: invalid declared tools render as removable chips
    (MUI multi-select cannot deselect a disabled option, so the removal
    needs its own surface) — one Chip row inside the existing alert.
    The editor is already the node-level counterpart of AgentEditor's
    picker. Remove together with the AgentEditor tool-picker exemption.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/476
  ceiling: 91
- check: architecture.file_budget
  path: server/app/routes/instance_settings_contracts.py
  reason: >-
    Issue #509/#554/#561 folds three capacity knob blocks (agent_enqueue,
    result_unpack, agent_claim) into the instance settings document: three
    small nested contract models plus three fields on
    InstanceSettingsDocument. The nested
    models must sit beside their siblings (InstanceWorkflowsSettings /
    InstanceAgentWorkersSettings) — the document IS the admin API surface;
    splitting two-field blocks into a sister module would fork the contract
    from the file that owns its shape. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when the document gets a structural
    split (per-block modules) or the next retirement frees headroom.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/509
  ceiling: 58
- check: architecture.file_budget
  path: server/app/agent_broker/result_unpack_pool.py
  reason: >-
    Issue #554 adds configure() (the instance-settings pin) and the
    configured-value branch in _pool_size next to the env override it sits
    behind — the size-resolution precedence (env > instance setting > auto)
    must stay in one function to stay auditable; splitting two lines of
    precedence out would fork the policy from the pool it sizes. Ceiling at
    actual + 10 per the dated-exemption convention. Remove when the env
    override channel retires (transitional per #554).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/554
  ceiling: 100
- check: architecture.file_budget
  path: server/app/main.py
  reason: >-
    Issue #554: one import plus one result_unpack_pool.configure() call right
    after apply_instance_settings — the composition root is where hydration
    is sequenced before any result can arrive; moving the call elsewhere
    would hide the ordering invariant. Ceiling at actual + 10 per the
    dated-exemption convention. Remove when startup wiring gets its planned
    restructure or the env-override retirement re-budgets the module.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/554
  ceiling: 235
- check: architecture.file_budget
  path: server/app/configuration/executor_knobs.py
  reason: >-
    #555's AgentClaimConfig (worker_touch_interval_seconds throttle knob)
    lands on the same knobs module #554's ResultUnpackConfig just grew —
    four effective lines over the 45 ceiling. The knob-block-per-plane form
    is the module's stated layout (#188 neutral layer), so splitting one
    block out for four lines would fork the single home of executor_runtime
    knobs. Ceiling at actual + 3 (tighter than the +10 convention — the
    module only takes whole knob blocks). Remove when the knob rides the
    instance-settings intake (#561) or the budget re-balance pass re-budgets
    the module (tracked by the remove_when issue).
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/549
  ceiling: 50
- check: architecture.file_budget
  path: worker/execution/reactor_stream.py
  reason: >-
    #578 review rounds (subagent P0/P1 + codex P1/P2) grew the fail-closed
    takeover past the first-registration ceiling: dropped-fence check inside
    takeover, blocking-mode restore, and done-signaling from the fallback
    pump thread's exit are all correctness requirements, not padding. The
    stream bookkeeping + takeover belong together (the fence is defined on
    the same lock the takeover must respect) — splitting them would fork the
    single concurrency domain. Ceiling at actual + 5 per the dated-exemption
    convention. Remove when the #578 phase-3 cleanup retires the
    per-execution thread pump and folds the takeover path away with it.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/578
  ceiling: 101
- check: architecture.file_budget
  path: server/app/events/bus.py
  reason: >-
    #563 + codex review: the overflow policy gained per-channel semantics
    (snapshot channels drop-oldest with an evict threshold, incremental
    channels keep the immediate evict) — the counter dict, the policy
    parameter, and the semantics docstring are the whole feature. The bus
    is a single concurrency surface shared by every channel kind; moving
    one policy arm out would split the overflow handling across files.
    Ceiling at actual + 5 per the dated-exemption convention. Remove when
    the resync-signal follow-up (overflow -> typed signal instead of evict)
    re-budgets the overflow path.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/563
  ceiling: 96
- check: architecture.file_budget
  path: server/app/studio_chat/acp_session.py
  reason: >-
    #558 review rounds added request_stop() next to close(): the dead-token
    escalation needs the turn-end stop semantics (queue a _CLOSE without
    joining — keepalive runs ON the ACP thread, so close()'s join is a
    self-deadlock), and it must live beside close()/the _closed gate it
    shares. The correctness docstring (turn-end semantics, bounded waits,
    resume-teardown interplay) is the review's deliverable, not padding.
    Ceiling at actual + 5 per the dated-exemption convention. Remove when
    the studio-chat lifecycle surface gets its planned consolidation.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/558
  ceiling: 273
- check: architecture.file_budget
  path: server/app/studio_chat/events.py
  reason: >-
    #558 review P2: on_error's fatal arm gained the 'starting' exclusion
    (stale kill echoes must not fail a resume claim's first hop — same
    final_statuses semantics as on_exit). Three effective lines over the
    first-registration ceiling; the guard and its comment are the whole
    fix. Ceiling at actual + 5 per the dated-exemption convention. Remove
    when the studio-chat lifecycle surface gets its planned consolidation.
  owner: workspace-executor
  remove_when: issues/open/github.com/LuciusCao/agent-legion/issues/558
  ceiling: 136
